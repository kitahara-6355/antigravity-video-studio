# 引き継ぎ 2026-08-02（第2版）

**これ1つを読めば再開できる。** 他は必要になってから読むこと。

自動で入るもの（読みに行かなくてよい）:

- **憲法** — `CLAUDE.md` がセッション開始時に読まれる
- **現在地** — SessionStart フックが正典から注入する（実現度・現フェーズ・終了条件）
- **記憶の索引** — `MEMORY.md`。個別ファイルは必要なときだけ開く

手順は `.claude/skills/` にある（`/phase-run`・`/vision-audit`・`/gate-report`）。
**呼ぶまで読み込まれない。** フェーズ作業を始めるときに `/phase-run` を呼べばよい。

この文書には**このセッション固有の状態だけ**を書く。判断基準・手法・恒常的な事実は
記憶ファイル側にある。前版（初版）はこの文書で置き換えた。

---

## 1. いますぐ確認すること

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio && git fetch -q origin && git log --oneline origin/main -1 && gh pr list -R kitahara-6355/antigravity-video-studio --state open
```

`gh` は PATH に無いので `export PATH="$PATH:/c/Program Files/GitHub CLI"` を先に打つ。

**PR #39 が飛行中**（UX 逆向き索引の再生成、auto-merge 予約済み）。
BEHIND なら `gh pr update-branch 39`。着地したら Phase 1 の C-4 が完了する。

---

## 2. いまの位置

**Phase 1「ux_guarantee_owner」の途中。** 直前に Phase 0（正典の再建）を完了して
ゲート報告を出し、ユーザーが `ux_guarantee_owner` を承認した。

ビジョン実現度 **71.2%**。Phase 0 でサマリー欄の不整合（60 表示）を直したもので、
**スコアは動いていない**。

### Phase 1 の終了条件（ユーザー未確認のまま着手している）

| | 条件 | 状態 |
|---|---|---|
| C-1 | 検証項目の実行系が存在し、コマンド1つで L1 を判定できる | **未着手（次はここ）** |
| C-2 | Owner の L1 122項目すべてに PASS/FAIL が付く（SKIP ゼロ） | 未 |
| C-3 | 結果がスナップショットとして保存され、ラチェットで比較できる | 未 |
| C-4 | 逆向き索引の欠落 105件を解消 | ✅ PR #39 |
| C-5 | 既存 FAIL 16件（O-1 に14 / O-8 に2）を実在の不具合か古い判定か切り分け | 未 |

**予算は不要**（L1 は DOM 存在確認のみ。パイプラインを起動しない限り課金しない）。
L3/L4 の一部と L5 は Phase 1 のスコープ外。

条件はユーザーに提示済みだが明示的な承認は取っていない。異論が出たら組み替える。

---

## 3. 測り直さなくてよい実測値（2026-08-02）

再取得に時間がかかるものだけ載せる。

| 項目 | 値 | 出どころ |
|---|---|---|
| カバレッジ | line **75.4%**（53,217/70,535） | CI main `e02972c` / run `30732518139` |
| テスト | **9,244件 失敗0 スキップ12** | 同上 |
| branch coverage | **測れない** | CI が `--cov-branch` を採っていない（D-22） |
| フロント build | **成功**（vite 7.3.6 / 2,393 modules / 9.00s） | 手元。CI は無い |
| フロント lint | 46 errors / 6 warnings | main と同数 |
| UX 実行率（Owner） | **16.5%**（550中91件） | `snapshots/v8_baseline.json`（2026-06-28） |
| Owner L1 項目数 | **122**（`dom_exists`） | stories 実測 |

---

## 4. 未処理の宿題

**着手順。上から片付ける。**

1. **`gate-verifier` による Phase 0 の独立検証が未実施。** 憲法第4条を満たしていない。
   定義が PR #37 で入ったので、いまは使える。C-1 と並行で回す
2. **ラチェットのベースライン締め直し。** 第1層は 16→0 になった（PR #33〜#36）が、
   どの PR でも `--update` していない。**16パス分の余白が残っており、再流入しても
   気づけない。** main の CI アーティファクトから実測を取って
   `python .github/scripts/fs_guard_ratchet.py --update`
3. **`script/.claude/` は Git 管理外。** `settings.json` と `hooks/launch.py` が
   壊れても戻せない。`launch.py` はフック実体が無いブランチでも落ちないための
   ランチャーで、ポリシーは持たない（実体は `antigravity-video-studio/.claude/`）。
   **これが無いと `.claude/` を持たないコミットへ checkout した瞬間に Bash が全面的に止まる**
4. 前セッションの worktree が3つ登録されたまま
   （`152f86ec-.../scratchpad/wt-{main,pr15,pr8}`）。実体も残っている

---

## 5. 触ると危ないもの

- **`docs/ux_fulfillment_matrix.md` を現在地の根拠にしない。** 2026-04-30 で止まっており、
  「5層未定義」の記述は事実と違う（D-21）
- **`backend/.env` が無い。** 実 API キーが未設定なので、E2E 軸はそもそも実行できない（D-20）
- **テスト実行は本番ファイルを汚す。** 走らせた後に `git status` を見て、
  意図しない差分は `git checkout --` で戻す（承認不要）。
  第1層は潰したが、PR が全部着地するまでは残っている可能性がある
- **CI の診断ステップ**（`ci.yml` の「Linux 固有失敗の診断」「chaos 汚染源の二分探索」）は
  調査用の一時ステップ。`continue-on-error` 付き。原因が出揃ったら削除してよい

---

## 6. ユーザー側の宿題（私からは操作できない）

**催促は1回で十分。**

| 項目 | 状態 |
|---|---|
| Streamlit Secrets のパスワード変更 | 未。**公開済みの平文は削除では取り消せない** |
| `dx-nyasan/jigyokei-app` の削除 | 未。削除して失うものは 0 コミットと実測済み |
| `dx-nyasan/jigyokei-copilot` の削除 | 未。**先に `git clone --mirror` で移送が必須**（509コミット・72ブランチ） |
| GitHub `dx-nyasan` の連絡先メール変更 | 未。GWS テナント解約より**先**に |

GitHub Organization への移行は「ラチェットが完了して main が緑で固定されたとき」が
適切な区切り、と合意済み。**いまは着手しない。**

---

## 7. Dependabot（変化なし）

| PR | 判断 |
|---|---|
| [#15](https://github.com/kitahara-6355/antigravity-video-studio/pull/15) eslint 10 | 現状インストール不可（`npm ci` が ERESOLVE）。`eslint-plugin-react-hooks@7.1.1` を一緒に上げれば通る見込み |
| vite 8 | **不要。** high は 7.3.5 で patched、PR #23 で 7.3.6 に更新済み |
| react-router | **`^` 範囲内では塞げない。** import の書き換えが要るので Dependabot が PR を作れない |
