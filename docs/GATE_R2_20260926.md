# ゲート報告 — R2 承認工程を必須にする（M1g・2026-09-26・クラウドのセッション）

## 1. 判断してほしいこと

> R2 のゲートを通し、**R2.5（G1: raw 4本の1本目）** に進んでよいか。
> 同時に、憲法の第1回改訂（仕分け表 `docs/specs/2026-09-26-constitution-triage-draft.md`）と、raw 素材の保管場所（M0 の残り）を決めてほしい。

前提: **C1〜C3 は本物の置き場（`vault-outputs/`）での `--gate` exit 0 がローカルに残っている**（クラウドには置き場が無い）。正典の `met` は C5 だけ立て、C1〜C3 はその確認の後に立てる。

## 2. 終了条件の充足

| 条件 | 判定 | 証拠 |
|---|:---:|---|
| **R2-C1** 承認していない動画は書き出せない | ✅（ローカル確認待ち） | gate-verifier 10周目（HEAD `06570e2`）で met。一時の置き場で本物の CLI と `RenderWorker`（ffmpeg）を通し、承認なし `--export` rc=1・承認後 rc=0・置き場の監査 rc=0。中間ゲート報告 `docs/GATE_R2_INTERIM_C1-C3_20260926.md` |
| **R2-C2** 承認の証跡が動画ごとに残る | ✅ | 同上。`--trace` に時刻・承認者・人が直した差分（unified diff）・開示・書き出し |
| **R2-C3** 開示が欠けたら書き出しが止まる | ✅ | 同上。サイドカーの書き込みに ENOSPC を注入すると rc=1・完成品は捨てられる。置き場の監査も開示を求める |
| **R2-C5** 承認画面でモデルが見え、その場で昇格してやり直せる。なぜそのモデルかが記録と画面に | ✅ | gate-verifier **5周目**（HEAD `eccaca4`）で met。実ブラウザで「見る → 1段上げる → 同じ素材でやり直す → 新しい実走が premium で採用 1 件」まで1画面（`docs/evidence/r2c5_verifier5_one_screen_20260926.png`）。429 の降格が記録と画面に「降格 3.6-flash → 3.5-flash-lite（429:枠枯渇）」と出る |

**C5 の検証の経緯（5周）**: 1周目 U1・U2（枠チェックの降格と呼び出しゼロが「宣言どおり」）→ 2〜4周目は同じ類（AI の出力が提案に届いていないのに「宣言どおり」）が経路を変えて3回 → ユーザー決定で**作りを変えた**（採用した AI の出力の件数を記録に残し、ゼロなら stub）→ 5周目 met。範囲外は limits #23〜#28 に宣言。

**退行の確認**
- 関連テスト（cost_guard / run_record / model_governance_backoff / approval_gate / r2_approval_router / agents_run_record / ai_proofreader / proofread_worker）はすべて緑。`test_workers/test_youtube_opt_worker.py` の6件と `test_main_coverage.py` の2件は **main でも同じ失敗**
- 台帳ゲート（c4_inventory / export_sites / ui_api / closure / ratchet / feature_gaps / ruff）は緑。`c4_response_gate` は環境差（`GET /api/models`）を除いて HEAD と同じ
- **CI**: PR #86 の最終 head `3ecd6e4` は4ジョブ緑（run 36247936735・§8）

## 3. スコアの変化

**未計測。** `/vision-audit` は本線の実走（実キー）を含むので、クラウドでは回していない。R2 で直接動く軸は `progressive_preview`（30 → 承認の強制が入った）と `ux_guarantee_owner`（承認画面）。**実測はローカルで**（M0 の後に R2.5 の実走と合わせて1回）。

## 4. このフェーズで入れた変更

- PR [#85](https://github.com/kitahara-6355/antigravity-video-studio/pull/85)（マージ済み `dce8d22`）: 本線を提案で止め、承認と書き出しの門・証跡・開示・置き場の監査（C1〜C3・24コミット）
- PR [#86](https://github.com/kitahara-6355/antigravity-video-studio/pull/86)（`cc/r2-c5`・12コミット）: D-39（降格の理由を記録に）→ 読み取り画面 → 画面から承認 → 昇格してやり直す → 検証5周の修正と作りの変更
- 計画書 `docs/plans/2026-09-26-master-plan-100.md`（承認済み）・正典 `roadmap.plan`・`.claude/budget.json` の枠・M0 手順書・憲法の仕分け案

## 5. 想定と違ったこと

- **「宣言どおり」を断定していた。** 記録は「宣言したモデル」を書き、実測は台帳の行から拾うだけだった。AI の出力が提案に届いていない経路（枠チェックの降格・呼び出しゼロ・応答を捨てたスタブ・解析失敗・リスト形で全部捨てる）が5つ見つかり、経路を塞ぐ限り次が出た。**証拠（採用件数）で決める作りに変えるまで4周かかった** — 計画の段で「宣言どおりの定義」を書いていれば1周で済んだ
- **`cost_guard` / `model_policy` は `backend.` 側と素の側で別のモジュール実体**。片方だけ差し替えると本番の台帳・上書きに書く。1回踏んで `git checkout` で戻した（引継ぎ §0.5 に罠として記載）
- **GCS は請求先が必須**で `avs-prod-free` には置けない → 保管場所の決定を M1g に持ち越した
- クラウドのコンテナは pytest を2本同時に走らせると落ちる（1回再起動）

## 6. 次フェーズの提案

計画書どおり **R2.5（G1: raw 4本を結合した1本目・結合＋カット＋字幕）**。着手の前に決めてほしいこと:
1. **raw 素材の保管場所**（Cloudflare R2 無料枠 ／ GCS on avs-prod-paid ＋ 請求先の自動切断）— これが決まらないと R2.5-C5（クラウドだけで再現）が満たせない。当面はローカルで実走してもよい
2. **憲法の第1回改訂**（仕分け表）— 承認されたら改訂文を PR で出す
3. R2 のブランチ `cc/r2-c5` のマージ（CI 緑を確かめてから私がマージする — #85 と同じ扱いでよければ）

## 7. 積み残し

- **D-49**: `model_governance.call()`（quality_gate_ai）が cost_guard の guard と台帳を通らない（課金の絞り口の外・AI を効かせた時点で「未検証」と誤表示）。R2.5 の品質ゲート整備で塞ぐ
- limits #27: 字幕の焼き込み失敗が記録に出ない（`_subtitle_burn_failed.flag` を誰も読まない）— R2.5 の1本目で触る
- limits #28 の軽微3件（中身のある鍵の判定・分母の表示・古い実走の文言）
- C1〜C3 の本物の置き場での `--gate` 確認（ローカル）

## 8. CI（最終 head）

最終 head `3ecd6e4`（`93cf237` にゲート報告と引継ぎを足したもの・コードは `eccaca4` と同じ）: **run [36247936735](https://github.com/kitahara-6355/antigravity-video-studio/actions/runs/36247936735) — 4ジョブすべて成功**（2026-09-26 14:48Z 完了）。

| ジョブ | 結果 |
|---|:---:|
| Python テストスイート (3.13) | ✅ |
| testpaths 外の退行検知 | ✅ |
| コード品質ラチェット | ✅ |
| シークレットスキャン | ✅ |
