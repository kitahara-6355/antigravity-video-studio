# Improvement Backlog (積上げ改善タスク)

各フェーズの開発・テスト中に発見された、品質向上や技術的負債解消のための改善タスク一覧。
Phase 7完了後に総合評価（影響範囲分析等）を行い、実装の可否を決定する。

| ID | 発生フェーズ | 改善内容 | 優先度 | 影響範囲 |
| :--- | :--- | :--- | :--- | :--- |
| **IMP-001** | Phase 1 | `tests/test_api.py` の `client` フィクスチャが未実装（`pass`）のため、全APIテストがエラーになる。`main.py` から `app` をインポートして `TestClient(app)` を返すように修正が必要。 | 中 | 局所（テストのみ） |
