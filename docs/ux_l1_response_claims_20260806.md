# レスポンス内容の主張を判定する（2026-08-06）

P2 の終了条件 **C-2** に対する、`gate-verifier` の指摘への回答。

**指摘。** endpoint で判定していた 89 項目は 47 個のエンドポイントにしか対応して
おらず、そのうち 37 件は「statusフィールドが存在する」のような**レスポンス内容の
主張**なのに、判定しているのはエンドポイントの実在だけだった。この 37 件を
「主張が検証されていない」として除くと 83/122 = **68.0%** で 90% を割る。
つまり閾値の充足がこの 37 件に依存していた。

**対応。** 判定手段を足した。指摘は正しく、P1 で解消したはずの
「dom_exists のラベルで API を測る」偽 PASS が、経路の粒度に移っていただけだった。

```bash
python -m backend.ux_verification.executor --persona owner
```

| | 前 | 1回目の対応 | 2回目の対応 |
|---|---:|---:|---:|
| PASS | 120 | 112 | **112** |
| うち内容まで判定 (`field_found`) | 0 | 25 | **30** |
| FAIL | 2 | 10 | **10** |
| 充足率 | 98.36% | 91.80% | **91.80%** |
| **判定されていない内容の主張** | **37** | 6 | **1** |

**数字は下がったが、判定されていない主張は 37 件から 1 件に減った。**
判定した結果ダメだったものは PASS のままではなく FAIL として出ている。
`gate-verifier` の厳しい数え方（主張が検証されていないものを PASS から外す）でも
111/122 = **90.98%** で、C-2 の 90% を満たす。

残る1件は O1-L1-04「動画リストに対応拡張子のみ含まれる」。
これは**フィールドの有無ではなく値についての主張**なので、宣言できる
`response_field` が無い（§3.3）。

---

## 1. 何を見るようにしたか

`response_field` を宣言した項目は、ハンドラの返り値にそのフィールドが現れるかまで
判定する。現れなければ `field_not_found` で FAIL。

ルーターの `return` を読むだけでは足りなかった。実測すると本番のハンドラは
ほとんどが薄い受け皿で、中身はサービス層が組み立てている:

```python
# backend/routers/trinity.py:158
status = service.get_evolution_status()
return status                       # ← ここだけ見ても 0 フィールド
```

そこで (1) ハンドラ内のローカル代入を辿り、(2) 呼び先の関数を `backend/` 全体の
索引から引いて**一段だけ**展開する。結果は
`recommendation.recommended_segments.score` のようなドット区切りのパスで持ち、
**どこで見つけたかを証拠に書く**:

```
static_response_scan: routers/smartcut.py:189 POST /api/smartcut/recommend
  が recommendation.recommended_segments.score を返す
```

一段しか辿らないのは、辿り始めると呼び出しグラフ全体になり、
「どこで見つけたのか」を証拠として書けなくなるため。索引の構築は約 4 秒。

## 1.5 2回目の検証で出た2つの穴

1回目の対応を `gate-verifier` に再度かけたところ、**また not_ready** だった。
指摘は2件とも正しかった。

### 穴1 — 走査が辞書を1つ丸ごと見落としていた

`response_field` を宣言していない項目の中に、レスポンス内容の主張が
**まだ6件**残っていた。うち4件は `GET /api/render/settings` 1本に乗っている
（O8-L1-04 BGM設定 / O8-L1-05 LUFS設定 / O8-L1-06 ロゴ設定 / O8-L1-07 字幕設定）。

宣言すると**落ちた。** 原因は走査側の欠陥が2つ重なっていたこと:

```python
_render_settings: Dict[str, Any] = {   # ← AnnAssign。_module_dicts が
    "bgm_volume": 50.0, ...            #    ast.Assign しか見ていなかった
}

return {"settings": _render_settings.copy()}
#                                    ↑ 呼び先索引を "copy" で引いて空振り。
#                                      `.copy` を剥がして本体に降りていなかった
```

拾えるパスが `success` / `settings` の2本しかなく、**設定 11 フィールドが
まるごと見えていなかった。** どちらも直した結果、4件とも
`settings.bgm_volume` などで PASS する。実装は最初から正しかった。

宣言の有無を分けていた基準が「説明文に英字のフィールド名が literal で
書かれているか」であって主張の性質ではなかったことも、この指摘で分かった。
`O12-L1-07`「user_modelオブジェクト含む」は宣言されて FAIL になり、
`O8-L1-05`「LUFS設定が含まれる」は同じ型の主張なのに宣言されず PASS のままだった。

### 穴2 — `--tighten` が守りたかった退行を受理していた

`field_not_found` は「判定を厳しくした」ときにも「レスポンスから
フィールドが消えた」ときにも同じように出る。新しい理由コードだけを見ていたので、
**C-2 で新しく守れるようになった退行を、理由文を添えるだけで通せた。**

前回の判定理由も見るようにした。`field_found`（内容まで見て PASS）から
`field_not_found` に落ちたのは厳格化ではなく退行なので拒否する。
そのためベースラインに `reasons` を記録するようにした。
理由が無い古いベースラインに対しては、区別できないので**受理せず拒否する**
（区別できないものを通せば、それ自体が抜け道になる）。

## 1.6 3回目の検証 — ラチェットが判定の強さを守っていなかった

3回目も **not_ready**。指摘は2件、どちらも正しかった。

### 穴3 — 宣言を消すだけで、全部が無検出で巻き戻せた

`L1Ratchet.check()` は `items` の **verdict しか比較していなかった。**
ストーリーから `response_field` を消すと:

- `field_found` → `found`（**PASS のまま**なので verdict 比較では何も起きない）
- `field_not_found` → `found`（**FAIL → PASS**。ラチェットは「改善」と報告する）

実際に再現した（一時コピーで実行。実リポジトリは無改変）:

```
response_field を 38 件削除 → PASS 120 / FAIL 2 = 98.36%
ラチェット: valid=True / 違反 0 件
```

**C-2 でやったこと全部が、宣言を消すだけで緑のまま巻き戻り、しかも
充足率は 91.80% → 98.36% に「改善」して見えた。**

判定理由の強さも見るようにした。`field_found` / `field_not_found`（内容まで
見た）から外れたら `weakened` として違反にする。同じ再現を掛け直すと:

```
response_field を 38 件削除 → PASS 120 / FAIL 2 = 98.36%
ラチェット: valid=False / 違反 38 件（全部 weakened）/ 改善 0 件
```

### 穴4 — `--tighten` は理由の**項目単位**の欠落を素通りしていた

`reasons` を辞書ごと消せば拒否できていたが、`(before_reasons or {}).get(item_id)`
は**1項目分だけ消せば `None` を返す**ので、そこを通り抜けられた。
項目単位でも「前回の理由が分からない」は通さないようにした。

**ベースラインを手で書き換える経路は塞いでいない。** ベースラインはレビュー対象の
成果物で、書き換えは PR の差分に出る。機械で守るのはツール経由の経路まで。

### C-2 の残り — 走査範囲が endpoint 宣言項目に限られていた

`response_field` の検討を endpoint を持つ 89件に限っていたため、
**testid 側に同じ型の偽 PASS が残っていた。** 最も明白なのが
`O1-L1-01`「動画一覧APIが正常応答を返す」で、`data-testid=video-file-browser`
の実在だけで PASS していた。`api_contract.py` の冒頭が「形だけ data-testid を
足して PASS にすれば偽 PASS が構造的に成立する」と書いている、その形そのもの。

C-4 の切り分け表（`docs/ux_l1_remaining_fails_20260803.md`）は O-1 の
testid 付き API 主張を 5件（O1-L1-02/03/04/06/07）として洗い出していたが、
**O1-L1-01 が数え漏れていた。** PR #52 で承認済みの「O-1 の照合先を実体に
合わせる」に含まれるべきだった1件なので、同じ扱いで `endpoint` に直した。

## 2. ラチェットとの関係 — 「厳しくした」を機械で区別する

判定を厳しくすると PASS が減る。実装の退行と見分けが付かない。
かといって「厳しくしたから」で何でも通せるようにすると、ラチェットが機能しない。

そこで `--tighten` を足した。2つの側から絞る:

1. **新しい判定理由**が `field_not_found` のものだけ。`not_found`（実体が消えた）・
   `unregistered`（登録が外れた）・項目の削除が混じっていれば拒否
2. **前回の判定理由**が `field_found`（内容まで見て PASS）でないこと。
   そこから落ちたのは厳格化ではなく、レスポンスからフィールドが消えたということ

理由は必須で、ベースラインに履歴として残る:

```json
"tightenings": [
  { "reason": "レスポンス内容の主張を…判定するようにした（P2 C-2）",
    "items": ["O11-L1-10", "O12-L1-07", ...] }
]
```

履歴を消さないのは、消すと「昔は緑だった」が見えなくなり、退行と区別が
付かなくなるため。`--tighten` を使わなければ、同じ差分は普通に違反として止まる。

## 3. 残る 8 件の切り分け

**実在の欠落は 0 件。** 5 件は照合先の書き方が実装と食い違っており、
3 件は静的走査では原理的に判定できない。

### 3.1 仕様の穴 — 機能は実在。照合先が違う（5件）

| 項目 | 主張 | 宣言された照合先 | 実体 |
|---|---|---|---|
| O11-L1-10 | `hook_score` 含む | `POST /api/youtube/pre-plan` | **`POST /api/youtube/optimize`** が返す（`youtube_optimizer.py:629`） |
| O12-L1-07 | `user_model` オブジェクト含む | `GET /api/evolution/status` | **`GET /status`** が `branding_manager.user_model` を丸ごと返す（`trinity.py:53`）。レスポンス**そのもの**であってフィールドではない |
| O12-L1-08 | `result` 含む | `POST /api/analytics/sync` | レスポンス自体が result（`biz_xp` / `quests` / `rivals` / `stats`）。**`result` は変数名であってフィールド名ではない** |
| O6-L1-05 | 4カテゴリ(audio/video/subtitle/structure)が存在する | `GET /api/pipeline/quality-gate/scores` | `categories[].id` の**値**。フィールド名ではない（拾えるのは `categories.id` / `categories.name` / `categories.score` …） |
| O8-L1-10 | `output_file` 存在 | `GET /api/render/status/{job_id}` | **`POST /api/render/complete/{job_id}`** が返す（`render.py:315`）。status 側は `stages` / `progress` / `current_stage` などを返す |

いずれも**実装は在る。** 直すべきはストーリー側の照合先だが、
これは仕様の書き換えなので実行しない（C-4 と同じ扱い。§5 参照）。

### 3.2 判定不能 — 静的走査の原理的限界（3件）

| 項目 | 主張 | なぜ判定できないか |
|---|---|---|
| O12-L1-10 | `entries` 配列含む | `GET /api/evolution` は `branding_manager.get_evolution_log()` = `self._load_json(path)` を返す。**レスポンスの形はディスク上のデータが決めており、コードには書かれていない** |
| O4-L1-11 | 候補に `type` フィールドが存在する | `get_all_candidates()` は `{"highlights": self._context.all_highlights, ...}` を返す。**要素の型は実行時のオブジェクトで、静的には追えない** |
| O4-L1-12 | 候補に `timestamp` フィールドが存在する | 同上 |

**これらを PASS にする方法はある——`response_field` を書かなければよい。**
書かなければ経路の実在だけで PASS になる。それをしないのは、
判定していないものを緑にするのが今回潰した偽 PASS そのものだから。

### 3.3 宣言できるフィールドが無い（1件・PASS のまま残る）

**O1-L1-04「動画リストに対応拡張子のみ含まれる」。**

`GET /api/pipeline/videos` は `videos` / `count` / `error` を返す。主張は
「`videos` の各要素の拡張子が `.mp4` などに限られる」という**値の条件**で、
フィールドの有無ではない。書ける `response_field` が無い。

同じ型の主張である O6-L1-05（audio/video/subtitle/structure は
`categories[].id` の値）は、たまたま値がフィールド名に見えたので宣言でき、
FAIL として出ている。**判定の一貫性という意味ではこの2件は同じ扱いが正しい。**

**この1件だけは、内容の主張なのに経路の実在で PASS している。**
`gate-verifier` の数え方でこれを PASS から外すと 111/122 = 90.98% で、
それでも C-2 の 90% は満たす。値の条件を判定するには実行時の応答を見る層が要る。

## 4. 判定の限界（新しく増えた分）

`api_contract.py` の既存の限界に加えて:

- **呼び先は一段しか辿らない。** サービスがさらに別のサービスを呼んで組み立てて
  いる場合は見えない
- **呼び先は関数名だけで引く。** 同名の関数が複数あれば**和集合**になり、
  偽 PASS 側に倒れる。厳密な解決には import の追跡が要り、静的走査の範囲を超える。
  そのため証拠にはパスを残し、どこで当たったかを追えるようにしてある
- **末端の名前でも一致させる。** ストーリーの多くは位置を書いておらず
  「レスポンスのどこかに在る」ことまでしか主張していないため。
  位置まで主張したい項目は `recommendation.score` のようにドットで書けば
  完全一致で判定される
- **同じ変数に複数回代入していたら最初の1つだけを使う。** どれが返るかは
  静的には決まらないので、後の代入で「見つかったことにする」のを避けた

## 4.5 判定手段が主張に対応していない 7 件（C-2 が未達である理由）

3回目の検証で数え直した結果、**PASS 112件のうち 7件は判定手段が主張に
対応していない。** いずれも「静的走査で判定できないから PASS にしている」のではなく、
**その主張を判定する手段がそもそも存在しない。**

| 項目 | 主張 | いま判定しているもの | 要る判定手段 |
|---|---|---|---|
| O1-L1-04 | 動画リストに対応拡張子のみ含まれる | 経路の実在 | 値の条件 |
| O1-L1-07 | localStorage に履歴キーが存在する | `recent-videos-section` の実在 | localStorage キーの静的走査 |
| O2-L1-07 | セグメントが3件以上表示される | `segment-item-*` 接頭辞1つの実在 | 要素数 |
| O4-L1-04 | 推奨APIが目標尺パラメータを受け付ける | 経路の実在 | リクエスト契約 |
| O4-L1-05 | 4種プリセット(15/30/45/60分)で正常応答する | 経路の実在 | パラメータ網羅 |
| O5-L1-06 | 推奨APIで再計算が可能(undo相当) | 経路の実在 | 冪等性・状態遷移 |
| O5-L1-07 | 同一パラメータで再取得が可能(redo相当) | 経路の実在 | 同上 |

**この7件を PASS から外すと 105/122 = 86.07% で、C-2 の 90% に届かない。**

O1-L1-07 の localStorage キーは静的走査で判定できる（実装は
`ProductionPipeline.jsx:38,50` に在る）。残る6件は要素数・リクエスト契約・
冪等性という別種の判定で、**P2 のスコープ「API 契約の実行系」の外**にある。

判定手段を足すか、C-2 の数え方を決め直すかは**フェーズの定義に関わるので
ユーザーの判断を仰ぐ**（憲法第1条）。推測で `response_field` を書いて
PASS を作ることはしない。

## 5. ユーザーの判断を仰ぐこと

§3.1 の 5 件は**照合先を実体に合わせれば PASS になる**（充足率 91.80% → 95.90%）。
ただしこれは仕様の書き換えなので、実行前に承認が要る。
P2 で O-1 の 5 件に同じことをしたとき（PR #52）も同じ手順を踏んだ。

**`test_method` と説明文は書き換えない。** 項目の削除も起きないのでラチェットには
抵触しないが、**誤った仕様を消す**操作であることは変わらない。

§3.2 の 3 件は、静的走査では判定手段が無い。実行時の応答を見る層（L2 以降）が
要るが、それは `backend/.env` が無いため現状そもそも実行できない（D-20）。
