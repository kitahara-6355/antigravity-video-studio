# 【保管庫】引継ぎ 2026-08-27 — R1.5 中間ゲートと C3

> **これは保管庫。現役は `docs/HANDOVER_20260828.md`。**
> ここに書いてあることは決着済みで、開かなくてよい。

**読むのはこの1枚だけ。** `CLAUDE.md` と SessionStart フック（ロードマップ）は自動で入る。
`docs/HANDOVER_20260826.md` 以前は**経緯の保管庫。開かなくていい**（決着済み）。

---

## 0. 手元の状態

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
git branch --show-current    # cc/r15-mainline
git log --oneline -1         # 2e02227
git status --short           # 空のはず
```

| | |
|---|---|
| main | `8eef716`（**中間ゲート承認済み。PR #80 は fast-forward でマージ済み**）|
| 作業ブランチ | `cc/r15-mainline` / PR **#81**（draft）|
| **CI** | 直近の緑は run **33076508731**（テスト **10,150件 失敗0** / カバレッジ 75.8%）|
| 予算 | `R1.5-mainline` **¥35.33 / ¥1,000**（無料枠なので実費は ¥0）|
| 実行記録 | `output/runs/` に **15件**。**消さない** |

- **`backend/.env` に実キーが入っている。触らない・読まない**
- **CLI は `PYTHONPATH=./backend` が要る**
- C5 用の素材は受領済み: `output/testclips/2025-09-22_16-34-33.mp4`（**約30分**・1280x720・h264+aac）

---

## 1. いますぐやること

### **C4・C5・C6 を `gate-verifier` に検証させ、フェーズのゲート報告を出す。**

中間ゲート（C1a〜C1d・C2）は 2026-08-27 に承認・マージ済み（main = `8eef716`）。
**C3 は met**（2周かかった）。**C4・C5・C6 は実装が終わっているが met はまだ立てていない**
（憲法第4条: 自分の成果を自分で採点しない）。

```bash
# C4
cat docs/C4_DEPENDENCY_RUN_20260828.md
# C5
PYTHONPATH=./backend python -m backend.cost_guard --status
cat docs/C5_COST_MEASUREMENT_20260828.md
# C6
PYTHONPATH=./backend python -m backend.model_policy --sunset   # 本番 0 箇所 / exit 0
```

3つとも met になれば **R1.5 のフェーズ終了**。ゲート報告を出して止まる。

---

## 2. R1.5 前半で何が起きたか（`eadff10` → `2e02227`）

### 旧 R1.5-C1 は4つに分割した（2026-08-27 ユーザー決定）

旧 C1 は「一本化／記録／握り潰さない／**成果物ゲートが PASS する**」を1条件に束ねていて、
`gate-verifier` の2周をここで使い切った。**最後の節が形として間違っていた** —
「ゲートが PASS すること」を条件にすると**ゲートを弱くするのが最短の達成手段**になり、
実際そうなっていた。C1c で「**赤を FAIL にできること**」へ書き換えたので基準は下がっていない。

| 条件 | met | 中身 |
|---|:--:|---|
| C1a | ✅ | 実行記録が再開材料になっている（工程・実モデル ID・原因・入力）|
| C1b | ✅ | 動かなかった工程を成功に数えない |
| C1c | ✅ | 成果物ゲートが**赤い実走を FAIL にする**（`degraded` は落とさない）|
| C1d | ✅ | 本線が agents 側に一本化（再開案内・「唯一の入口」）|
| C2 | ✅ | モデル解決器が1つ |

### コミット

| コミット | 内容 |
|---|---|
| `63d6e72` | **C2**: 解決器の一本化。`--audit` に `resolver_split` |
| `d675039` `6693979` | **C1**: 本線に `RunRecorder` と失敗の伝播 |
| `0502297` | 1周目の指摘5件 |
| `2f2fa82` | **C2 を met に**（正典2箇所）+ limits 2件 |
| `546fb6d` | **C1 を4分割**し、2周目の指摘3件（N-1/N-2/N-3）を塞ぐ |
| `fd98b26` `29f094d` | 実走で確認 / `check_models` にも同じ穴があったので塞ぐ |
| `855a443` `f5f1e29` | **C1b**: 致命的失敗の記録・改善ループの迂回・`None` 返し |
| `2e02227` | **C1b を met に** + limits 2件 |

### 本線の実走（証拠）

```
AVS_SKIP_LEARNING_SIDE_EFFECTS=1 PYTHONPATH=./backend \
  python -m backend.agents.pipeline_coordinator output/testclips/r1_clip30s.mp4
```

最新: `20260827T034428029331-0000` / `degraded` / **74.7秒** / ¥3.8033 / **Gemini 2回**。
`health.failed_stages = ["quality_gate"]` / `all_features_active: false`。

**`degraded` の原因は品質スコアが 2/100 で安定して落ちること。** 改善ループ3周でも動かない。
30秒クリップだからか判定が厳しすぎるのかは**未切り分け**。

---

## 2.5 中間ゲートの後にやったこと（2026-08-27・承認後）

### 品質スコア 2/100 の切り分け（ユーザー指示で C3 の前に実施）

原因は**3つ、それぞれ独立**していた。**主因は測定条件のミス**だった。

| 原因 | 減点 | 直した内容 |
|---|---|---|
| **目標尺のミスマッチ** | −50 | `--target-minutes` の既定20分を**素材の尺から自動決定**に。30秒素材に対して QV-01「出力尺異常（差19.6分）」が満額で乗っていた。**`target_minutes` は SmartCut では使われておらず、品質ゲートの期待値としてのみ効く** |
| **本線に無い工程を減点** | −20以上 | 本線にサムネイル工程は無い。**構成上どうやっても閾値90に届かない**ので、台帳に載っている機能は減点せず `skipped_features` に出す |
| **0で床打ち** | — | 減点合計 −134（素点 −34）でも表示は 0 点で、改善しても数字が動かなかった。素点を記録と出力に残す |

**実測: 2点 → 89点。** 閾値90まであと1点。残りは解像度720p など素材側の要因。

### 実装不足項目の台帳（ユーザー要望）

```bash
PYTHONPATH=./backend python -m backend.feature_gaps --show    # 一覧
PYTHONPATH=./backend python -m backend.feature_gaps --audit   # 点検（exit 1 で FAIL）
```

- 台帳 `backend/config/feature_gaps.json`（gap 6件 + intentional 1件）
- **一覧は台帳、期限は正典。** 正典の条件文は書き換えない
- **台帳に載っている機能は品質ゲートが減点しない。** 実装したら台帳から消え、
  **その瞬間からゲートが本気で見はじめる**（台帳が飾りにならない仕掛け）
- CI では `--audit --static-only`。**評価しなかった検査を必ず列挙する**
- 設計 `docs/specs/2026-08-27-feature-gaps-design.md` / 計画 `docs/plans/2026-08-27-feature-gaps.md`

**点検は初日に私の記載ミスを4件捕まえた**（パス誤り3件＋`shorts_candidates` は実は実装済み）。

### C3（AI の出力が成果物に届く）— 実装済み・**検証待ち**

**第1要件（SHA256 で機械判定）**: 実測で前提を確かめた。

| 実行 | AI | SHA256 | bytes |
|---|---|---|---|
| ×3本 | なし | `58b2511ab17a1ba2`（**3回とも一致**）| 2,988,566 |
| | あり | 毎回異なる | 2,986,662 ほか |

**AI なし同士が完全一致 → エンコーダは決定的**。差は AI に起因すると言い切れる。
`artifact_gate --ai-effect` が判定する。実行記録に `artifact_digests` を持たせた。

**第2要件（使われていない中間成果物が 0）**: `--intermediates` で数える。

`youtube_opt` の titles/tags/description は `ctx.metadata` に入るだけで**CLI 実行では
戻り値ごと消えていた**（消費者の YouTube 投稿が未実装のため）。
**最終動画の隣に `<final>.youtube.json` を書き出す**ことで 0 にした（2026-08-27 ユーザー決定）。
投稿が未実装な以上いまの現実は手動投稿なので、そのまま貼れる形で残す。
0 になったので `check_intermediates` を `--gate` に組み込んだ。

### C4・C5・C6（2026-08-28）

**C4（偽の success を止める）** — 4件を1つずつ実際に呼んだ記録が
`docs/C4_DEPENDENCY_RUN_20260828.md`。

| # | 直す前 | 直した後 |
|---|---|---|
| 投稿 | `success=True` / `placeholder_video_id` | `success=False` / `not_implemented` |
| チャンネル統計 | `watch_time_hours: 15200` を実測のように | `is_real: false` / `data_source: "sample"` を添える |
| 品質スコア | 未計測を「0.0/100」と表示 | 「未計測」と表示（**本線の品質スコアは動いている**。正典が指していたのは別経路） |
| retention | `random.random()` を `success=True` で返し、**サイドカーにまで載っていた** | `IMPLEMENTED = False` で飛ばす |

**C5（原価と所要時間）** — 30分素材で **completed / 20.0分 / ¥42.69 / AI 6回 / 品質94点**。
記録は `docs/C5_COST_MEASUREMENT_20260828.md`。

- **支配的なのはレンダリング（38%）**で文字起こし（29%）ではなかった（引継ぎの見込みは外れ）
- 原価は**ほぼ全額が校閲**（95%）。尺にほぼ比例する
- 1日の上限: RPD から **166本**／実時間から **72本** →**効いている制約は実時間**。無料枠には収まる
- `model_config.json` の段に RPM/RPD を記録した（**いま使っている4段の枠は1つも記録が無かった**）

**C6（2.5系サンセット）** — `--sunset` が**本番と到達不能を区別**するようにした。

- 「本番」は**本線と API から import を静的に辿れるもの**（`reachable_modules`）
- **コード上の値と文書を分ける**（docstring / コメント / `__main__` の自己テスト）
- 本番のコード参照 23箇所を `gemini-3.6-flash` に差し替え → **本番 0 箇所 / exit 0**
- 中身はすべて**古い据え置きフォールバック**（`def get_model(task): return "gemini-2.5-flash"` など）

**踏んだ罠**: 実行時状態の JSON を戻すための `git checkout -- backend/usage_tracker/` が、
**同じディレクトリの `tracker.py` への修正も巻き戻していた**。自分の実装に
変異テスト（本番に 2.5 を1つ戻してゲートが噛むか）をかけて見つけた。


---

## 3. gate-verifier の指摘と対処（2周ぶん）

**1周目（旧 C1・`d18ccd5`）で 3件、2周目（新 C1b・`29f094d`）で 2件。すべて「うっかり型」。**
どれも**自分で再現してから直した**。

| # | 指摘 | 対処 |
|---|---|---|
| N-1 | **ゲートが赤い実走を落とせない。** run の `status` を一度も見ておらず、動画も全 run から集めていたので1本前の mp4 が永久に緑を作った | `check_run_status()` を新設（緑は `completed` と `degraded` だけ）。動画と `models_used` も最新1本に |
| N-2 | **一度も動かなかった工程が `completed` に化ける** | 着手した工程は結果を返すまで「落ちた」扱い。事前フックの例外も数える |
| N-3 | **再開案内が旧パイプラインを指す** | `inputs.mainline` で分岐。**本線に再開は無い**ので、やり直しの入口を出す |
| 指摘1 | **致命的失敗のとき落ちた工程が記録に残らない。** `status: failed` なのに `all_features_active: true`、`--resume` が「失敗はありません」で exit 0 | `health.failed_stages` を `_outcomes` から導く。断られた工程は `_record_dead_stage()` で記録に1件残す |
| 指摘2 | **ガバナンスが禁じた工程を改善ループが迂回して実行していた** | `_ensure_allowed()` を通す |

加えて条件文が名指ししている「**工程が結果を返さない**」（`None` 返し）も塞いだ（`_normalized()`）。

**検証者は mutation test（修正を1つずつ外す）で、追加テスト全件が空振りしていないことまで確認している。**

---

## 4. 踏んだ罠（同じ所で止まらないために）

- **`pytest.ini` の testpaths を触るとバッチの区切りが変わり、既存のテスト汚染が別の場所で発火する。**
  **新規テストファイルを作らず、testpaths にある既存ファイルへ足すこと**（今回はそれで回避した）
  - 残っている汚染源: `test_admin_channel_router.py` / `test_context_compressor.py`。
    どちらも現在は緑のバッチにあるので触らない。根治は別 PR
  - 同型: `test_preview_engine.py:26` が import 時に `Path.is_file` をプロセス全体で `True` に固定する
- **テストが本番を汚すと `fs_guard` ラチェットが落ちる。** コーディネータを動かすテストは
  `_init_harness` と `_run_retention_analysis` を止め、`runs_dir` / `AVS_RUNS_DIR` を差し替える
- **`--audit` や `--gate` を1回叩くだけで `backend/usage_tracker/*.json` の日付が動く**（追跡下の実行時状態）。
  作業ツリーが汚れたら戻す
- **push するたびに CI が前の run を cancel する。** 連続で push すると
  「判定不能（test-results.xml 解析失敗）」が出るが、それは cancel された run の残骸
- **フルスイートはローカルで OOM する**（約13.6GB）。必ずファイル指定
- **実走は課金経路。** `AVS_SKIP_LEARNING_SIDE_EFFECTS=1` を付けないと DreamEngine が
  `VERIFIED_FACTS.md` などを書き換える

### 元から赤いテスト（`eadff10` でも赤い。退行ではない）

`test_harness_auditor.py`（7件）/ `test_e2e_pipeline.py::TestShortsGeneratorUI::test_shorts_generator_has_render_button` /
`tests/test_model_governance.py::test_resolve_model_edge_cases` / `test_gemini_client_factory.py` /
`test_stage_bound_agent.py` / `test_fitness_functions.py` / `test_quality_audit_fixes.py` /
`test_workers/test_youtube_opt_worker.py`(6) / `test_workers/test_quality_gate_worker.py`(2) /
`test_shared/test_cov_*` / `test_api_contract.py`(2)

> `tests/test_model_governance.py::test_resolve_model_edge_cases` が赤い理由は判明した:
> **同名関数が `:733` と `:994` にあり後者が前者を上書き**、期待値が `gemini-2.5-flash` のまま。
> config の `default_model` は `eadff10` の時点で `gemini-3.6-flash` だった。C2 の退行ではない

---

## 5. 承認後に着手すること（**承認前に着手しない**）

| 条件 | 状態 | 中身 |
|---|---|---|
| C3 | 未着手 | **AI の出力が成果物に届く。** 使われない中間成果物が 0 でなければ FAIL |
| C4 | 20% | **偽の success を止める。** 司令塔は C1 で済み。`youtube_uploader.upload_video()` は**未実装として失敗させる**（実装も削除もしない・2026-08-26 決定）。他に `admin_channel_router` の固定値・`quality_score` 常時 0.0 |
| C5 | 5% | **30分素材で原価と所要時間を測り直す**（素材は受領済み）。文字起こしが支配的で **15〜40分**の見込み |
| C6 | 未着手 | 2.5系サンセット（残り**50日**・2026-10-16）|

### 積み残し（作業チップを2件発行済み）

1. **最終レンダリングだけ事前フックを発火していない。** `render_final` を禁じるガバナンス
   ポリシーが黙って無視される。`_WORKER_SCOPE_MAP` には**書いてあるのに**呼ばれない
2. **`_close_recorder` が try/finally に入っていない。** 想定外の例外が抜けると記録が
   `running` のまま残り、`--resume` が exit 0 を返す。ディスク空き不足の早期 return が
   `all_features_active: true` を書く問題も同じチップ

### その他の申し送り

- **`--audit` はダミーキーだと exit 0 になる**（`model_config.json` に `verified: true` が
  保存済みのため）。**C6 で `--audit` を一次情報の証拠に使う前に直すこと。**
  C2 の解決器チェック自体はキー非依存で fail-closed
- **C1b の verify の片方（`test_pipeline_stage_failures.py`）は本線を通らない**
  （`backend.video_pipeline` を import している）。本線の検査は `test_agents_run_record.py` が担う。
  次に条件を書くときは本線を見るファイルだけ並べる
- **品質ゲートは規則ベース。** LLM は下位の `AIRuleCheck`（`quality_gate_ai`）が呼ぶ**任意経路**
  にしかなく、実走では**0回**。**AI を効かせるのは C3**
- 再監査 `/vision-audit` は R1.5 の後（2026-08-26 決定）

---

## 6. 検証コマンド

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
PYTHONPATH=./backend python -m pytest \
  backend/tests/test_vision_backlog_canon.py backend/tests/test_cost_guard.py \
  backend/tests/test_run_record.py backend/tests/test_revenue_artifact_gate.py \
  backend/tests/test_model_policy.py backend/tests/test_model_sunset.py \
  backend/tests/test_model_governance_backoff.py backend/tests/test_pipeline_run_record.py \
  backend/tests/test_pipeline_resume.py backend/tests/test_pipeline_stage_failures.py \
  backend/tests/test_model_resolver_unification.py backend/tests/test_agents_run_record.py \
  backend/tests/test_shared/test_model_governance.py \
  -q --no-cov          # 356 passed
```

```bash
PYTHONPATH=./backend python -m pytest backend/tests/test_workers/test_pipeline_coordinator.py \
  backend/tests/test_shared/test_pipeline_coordinator_coverage.py -q --no-cov   # 112 passed
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --gate          # exit 0（C1a の verify）
PYTHONPATH=./backend python -m backend.model_policy --audit                  # exit 0（C2 の verify・実キー）
PYTHONPATH=./backend python -m backend.model_policy --sunset                 # 2.5系（C6 の現在地）
PYTHONPATH=./backend python -m backend.cost_guard --status                   # 残高
python .github/scripts/ruff_ratchet.py                                       # 28842 → 28836 (-6)
python .github/scripts/fs_guard_ratchet.py
```

---

## 7. 進め方の約束

- **進め方の判断は確認不要。事後報告でよい**
- **`met` は自分で立てない。** `gate-verifier` に別コンテキストで検証させてから
- 同一条件への `gate-verifier` は**最大2周**。3周目の前に条件文を見直してユーザーに上げる
  （**2026-08-27 に実際にこれをやり、条件文の分割で解けた**）
- テストが3回連続で FAIL したら止めて報告する
- 有料利用は予算の範囲内なら都度確認不要（憲法第3条）。pro 昇格は別途承認
