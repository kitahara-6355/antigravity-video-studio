# 引継ぎ 2026-08-27 — R1.5 中間ゲート（C1+C2）の検証2周目から

**読むのはこの1枚だけ。** `CLAUDE.md` と SessionStart フック（ロードマップ）は自動で入る。
`docs/HANDOVER_20260826.md` 以前は**経緯の保管庫。開かなくていい**（決着済み）。

---

## 0. 手元の状態

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
git branch --show-current    # cc/r15-mainline
git log --oneline -1         # d18ccd5
git status --short           # 空のはず
```

| | |
|---|---|
| main | `eadff10`（R1 は PR #79 で fast-forward マージ済み）|
| 作業ブランチ | `cc/r15-mainline` / HEAD `d18ccd5` / PR **#80**（draft）|
| **CI** | run **33017227942** が `d18ccd5` で**3ジョブとも success**。マージゲート全条件クリア（テスト **10,110件 失敗0** / カバレッジ 75.8%）|
| 予算 | `R1.5-mainline` **¥7.91 / ¥1,000**（無料枠なので実費は ¥0）。`R1-execution-layer` は closed |
| 実行記録 | `output/runs/` に **6件**（R1 の4件 + 本線の実走2件）。**消さない** |

- **`backend/.env` に実キーが入っている。触らない・読まない**（`avs-prod-free` / 無料枠）
- **CLI は `PYTHONPATH=./backend` が要る**（裸 import が50箇所超）
- C5 用の素材は受領済み: `output/testclips/2025-09-22_16-34-33.mp4`（**約30分**・1280x720・h264+aac）

---

## 1. いますぐやること

### **`gate-verifier` に R1.5-C1 と C2 を検証させる（2周目・最終周）。**

前セッションで2周目を起動したが、**プロセス終了で結果を失った**（実装は完了・CI も緑）。
やり直す。**これが停止規則の最終周** — `not_ready` なら3周目に入らず、
条件文の見直しとしてユーザーに上げる（`verification_policy`）。

`ready` なら:
1. 正典 `backend/branding/vision_backlog.json` の R1.5-C1・C2 の `met` を `true` にする
   （**`current_phase` と `roadmap.phases["R1.5"]` の2箇所。片方だけだと canon テストが落ちる**）
2. 中間ゲートの報告を出す（`.claude/skills/gate-report` の構成）
3. **止まる。** `intermediate_gate` の決定により **C3 以降には入らない**

### 検証エージェントに渡す内容

`gate-verifier` はエージェント種別として登録済み（`subagent_type: "gate-verifier"`）。
プロンプトには最低限これを入れる:

- HEAD `d18ccd5` / CI run **33017227942**（3ジョブ success）を自分で確かめさせる
- **1周目（`f9f483e`）は C1・C2 とも `not_met`。指摘7点が塞がったかを確かめさせる**（下の §3）
- 禁止: 実走（課金）・`.env` 参照・`output/runs/` 削除・フルスイート（OOM 13.6GB）・
  リポジトリの書き換え・`backend/tests/test_model_governance.py` と
  `tests/test_model_governance.py` の同時実行
- **疑わせる点**: 成果物ゲートを最新1本に絞ったのは正当か / 既存テストの契約更新は
  「実装に合わせて緩めた」ではないか / 失敗の握り潰しに抜け道が残っていないか

---

## 2. R1.5 でここまでにやったこと（`eadff10` → `d18ccd5`）

| コミット | 内容 |
|---|---|
| `63d6e72` | **C2**: 解決器の一本化。`model_governance._resolve_declared()` を切り出し、段の解決を `model_policy` へ寄せた。`--audit` に `resolver_split` を追加 |
| `d675039` | **C1**: 本線（agents）に `RunRecorder` を被せ、失敗の伝播を作り直した |
| `6693979` | 本線の CLI・成果物ゲートの判定対象を最新1本に・テストが `output/runs` を汚さないよう `AVS_RUNS_DIR` で隔離 |
| `0502297` | gate-verifier 1周目の指摘5件を修正（§3）|
| `e5704a5` `b7ae1fd` `d18ccd5` | テスト汚染とラチェットの後始末（§4）|
| `32ff979` `fc6d8fb` | GitHub Actions の不発を抜けるための空コミット |

### 契約を作り直した（旧実装は最終レンダリングが落ちても `completed` を返していた）

| status | 条件 |
|---|---|
| `failed` | `transcribe` / `smart_cut` / `render` が落ちた → **中断**（動画が無い）|
| `degraded` | `proofread` / `preview` / `youtube_opt` / `quality_gate` が落ちた → 続行するが**完走とは呼ばない**。`health.skipped_features` に出る |
| `completed` | 全工程が通った |

判定は**最後の試行**で行う（`_outcomes`）。リトライで通ったものは失敗に数えない。
Hook 拒否（deny）も「動いていない」として数える。

### 本線の実走（証拠）

```
PYTHONPATH=./backend python -m backend.agents.pipeline_coordinator output/testclips/r1_clip30s.mp4
```

最新: `20260826T135356769360-0000` / `degraded` / **120.8秒** / ¥4.2658 / **Gemini 2回**
（`transcribe` 10.1s → `proofread` 36.4s → `smart_cut` 0.0s → `preview` 14.5s(4回) →
`youtube_opt` 40.6s → `quality_gate` 7.3s(5回) → `render` 9.4s）。
**C2 の修正前は本線で LLM が1回も通っていなかった**（全部 404）。

---

## 3. gate-verifier 1周目の指摘と、その後の対処

**1周目（`f9f483e`）は C1・C2 とも `not_met`。** 指摘7点のうち5件が本物だった。

| # | 指摘 | 対処 |
|---|---|---|
| 1 | CI が赤（起点比 +5 の新規失敗）| `d18ccd5` で緑（run 33017227942）|
| 2 | **記録が開けないと偽の success に戻る** — `_execute_worker` が早期 return して失敗を数えていなかった。最終レンダリングが落ちても `completed` | 記録の有無にかかわらず数える。`OSError` を注入する再現テストを追加 |
| 3 | **リトライ成功を失敗として数えていた** | `_outcomes`（最後の試行が答え）に変更 |
| 4 | 成果物ゲートを最新1本に絞ったのが PASS に効いている | **設計判断として維持**。理由は「過去の記録は直せないので、全件判定だと一度の失敗で永久に落ちる」。過去は `history_findings()` が `ℹ` として出す。**2周目でここを厳しく見られる** |
| 5 | `--resume-check` が検査していない件数まで数えていた | 検査した範囲だけ数え、残りは「過去 N 件は検査していません」と出す |
| 6 | **ガード2ファイルが testpaths 外で CI が一度も実行していなかった** | `pytest.ini` に登録（441 → 443）|
| 7 | `--audit` が `_resolve_declared` しか比べず、deprecated 差替の食い違いを見逃す | `validate_and_correct` まで含めて突き合わせ、テストで固定 |

**報告値の訂正**（前セッションで一度混線させた）: 実走は2本あり、
`135018` = 100.577秒/¥3.635、`135356` = 120.8秒/¥4.2658。予算消化は合計 ¥7.91。

---

## 4. 踏んだ罠（同じ所で止まらないために）

- **`pytest.ini` の testpaths を触るとバッチの区切りが変わり、既存のテスト汚染が別の場所で発火する。**
  `sys.modules["google.genai"] = MagicMock()` を import 時に置いて `errors` を置かないテストが
  複数あり、同じバッチの後続が
  `ModuleNotFoundError: No module named 'google.genai.errors'` で落ちる。
  **バッチの中身は `scripts/run_test_batches.py` の `read_testpaths()` + `split(files, 8)` で再現できる**
  - 塞いだ: `test_shorts.py`（バッチ7）/ `test_segments_router.py`
  - **残っている: `test_admin_channel_router.py` / `test_context_compressor.py`。
    どちらも現在は緑のバッチにあるので触らない**（単体では元から31件エラー）。根治は別 PR
- **同型の地雷がもう1つ**: `test_preview_engine.py:26` が import 時に
  `Path.is_file` をプロセス全体で `True` に固定する（初回コミットから存在・復元なし）。
  **根治は別 PR**（作業チップ発行済み・2026-08-26 ユーザー決定）
- **テストが本番を汚すと `fs_guard` ラチェットが落ちる**（「その他 116 → 118」で1回落ちた）。
  コーディネータを動かすテストは `_init_harness` と `_run_retention_analysis` を止める
- **GitHub Actions が不安定だった**（`startup_failure` 1回・20分 queued 1回・再実行不可）。
  ワークフロー定義は触っていない。詰まったら空コミットでイベントを起こし直す
- **フルスイートはローカルで OOM する**（約13.6GB）。必ずファイル指定
- **実走は課金経路。** `AVS_SKIP_LEARNING_SIDE_EFFECTS=1` を付けないと
  DreamEngine が `VERIFIED_FACTS.md` などを書き換える（2026-08-26 に1回起こした）

### 元から赤いテスト（`eadff10` でも赤い。退行ではない）

`test_harness_auditor.py`（7件）/ `test_e2e_pipeline.py::TestShortsGeneratorUI::test_shorts_generator_has_render_button` /
`tests/test_model_governance.py::test_resolve_model_edge_cases` / `test_gemini_client_factory.py` /
`test_stage_bound_agent.py` / `test_fitness_functions.py` / `test_quality_audit_fixes.py` /
`test_workers/test_youtube_opt_worker.py`(6) / `test_workers/test_quality_gate_worker.py`(2) /
`test_shared/test_cov_*` / `test_api_contract.py`(2)

---

## 5. 検証コマンド

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
PYTHONPATH=./backend python -m pytest \
  backend/tests/test_vision_backlog_canon.py backend/tests/test_cost_guard.py \
  backend/tests/test_run_record.py backend/tests/test_revenue_artifact_gate.py \
  backend/tests/test_model_policy.py backend/tests/test_model_sunset.py \
  backend/tests/test_model_governance_backoff.py backend/tests/test_pipeline_run_record.py \
  backend/tests/test_pipeline_resume.py backend/tests/test_pipeline_stage_failures.py \
  backend/tests/test_model_resolver_unification.py backend/tests/test_agents_run_record.py \
  backend/tests/test_shared/test_model_governance.py \
  -q --no-cov          # 338 passed
```

```bash
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --gate          # exit 0（C1 の verify）
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --resume-check  # exit 0
PYTHONPATH=./backend python -m backend.model_policy --audit                  # exit 0（C2 の verify・実キーで models.list 照合）
PYTHONPATH=./backend python -m backend.model_policy --sunset                 # 2.5系 71箇所（C6 の現在地）
PYTHONPATH=./backend python -m backend.cost_guard --status                   # 1本あたりの内訳
python .github/scripts/ruff_ratchet.py                                       # 28842 → 28836 (-6)
```

---

## 6. 中間ゲートの後（**承認前に着手しない**）

正典 `roadmap.phases["R1.5"]` の残り。進捗はおおむね **35%**（C1・C2 で土台は終わり）。

| 条件 | 状態 | 中身 |
|---|---|---|
| C3 | 未着手 | **AI の出力が成果物に届く。** 使われない中間成果物が 0 でなければ FAIL |
| C4 | 20% | **偽の success を止める。** 司令塔は C1 で済み。`youtube_uploader.upload_video()` は**未実装として失敗させる**（実装も削除もしない・2026-08-26 ユーザー決定）。他に `admin_channel_router` の固定値・`quality_score` 常時 0.0 |
| C5 | 5% | **30分素材で原価と所要時間を測り直す**（素材は受領済み）。文字起こしが支配的で **15〜40分**の見込み |
| C6 | 未着手 | 2.5系サンセット（残り**50日**・2026-10-16）。`--sunset` が本番モジュールとテストを区別できるまで FAIL |

### R1.5 で分かった、まだ手を付けていない事実

- **品質ゲートは規則ベース。** LLM は下位の `AIRuleCheck`（`quality_gate_ai`）が呼ぶ**任意経路**
  にしかなく、実走では**0回**だった。記録は `local:rule-based` に直してある。**AI を効かせるのは C3**
- **品質スコアが 2/100 で安定して落ちる。** 改善ループ3周でも動かない。
  30秒クリップだからか判定が厳しすぎるのかは**未切り分け**
- **本線に再開機能が無い。** R1-C4 の resume は `video_pipeline` 側の機能。
  `run_record --resume` の案内文も video_pipeline を指したまま
- `routers/segments.py` の後始末を直した（`file.close()` の HTTPException が
  成功結果を上書きしていた）。**テストは前からこれを主張していたが、汚染で実コードに届いていなかった**

---

## 7. 2026-08-26 のユーザー決定（7件・すべて反映済み）

1. R1 は直してから再判定 → 承認・マージ済み
2. **テスト汚染は最小で緑にし、根治は別 PR**
3. **再監査 `/vision-audit` は R1.5 の後**（いま測ると LLM が 404 のままの数字が確定するため）
4. **Monthly spend cap は pro 昇格の直前に置く**（`--status` の警告は先送りした決定）
5. **R1.5 の予算は新枠 ¥1,000**（見込みは ¥0）
6. **C5 は実運用に近い尺の実写素材で測る**
7. **C4 の `youtube_uploader` は未実装として失敗させる**

## 8. 進め方の約束

- **進め方の判断は確認不要。事後報告でよい**
- **`met` は自分で立てない。** `gate-verifier` に別コンテキストで検証させてから
- 同一条件への `gate-verifier` は**最大2周**。3周目の前に条件文を見直してユーザーに上げる
- テストが3回連続で FAIL したら止めて報告する
- 有料利用は予算の範囲内なら都度確認不要（憲法第3条）。pro 昇格は別途承認
