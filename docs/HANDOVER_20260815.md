# 引き継ぎ 2026-08-15（P5 実行中）

**これ1つを読めば再開できる。他のドキュメントを先回りして読まないこと。**
`docs/HANDOVER_20260810.md` は**この文書で置き換えた**（同じ日に削除）。

## 自動で入るもの（読みに行かなくてよい）

- **憲法** — `CLAUDE.md`（セッション開始時に読まれる）
- **現在地と P5 の終了条件 C-1〜C-6** — SessionStart フックが正典から注入する
- **記憶の索引** — `MEMORY.md`

手順は `.claude/skills/`（`/phase-run`・`/vision-audit`・`/gate-report`）。呼ぶまで読み込まれない。

---

## 1. いますぐやること

### **ユーザーの判断待ちで止まっている。C-1 の条件文をどう扱うか。**

8・9・10回目の反例を塞いだうえで、(2-B) の停止条件
「**塞ぐために綴りの列挙を増やすしか手が無い**」に当たったので止めた。
判断材料は §4-B にまとめてある。**私からは選択肢と実測を出すだけで、
条件文は勝手に緩めない**（憲法第1条）。

判断が出たら:

- **A案（`limits` に落とす）を選んだ場合** — 正典の C-1 に limits を足し、
  `exit_criteria[].met` を立て、11回目の `gate-verifier` を通してから
  ユーザーが `/gate-report` を打つ
- **B案（宣言の網羅性に書き直す）を選んだ場合** — `resolve.alias` と
  `dependencies` をベースラインに固定する実装を足す（見積り §4-B）
- **C案（到達可能性に作り替える）を選んだ場合** — 走査範囲の設計からやり直し。
  **P4 が14回破られた「精度を要求する判定」に戻る**ので、私は勧めない

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
export PATH="$PATH:/c/Program Files/GitHub CLI"
export GOOGLE_API_KEY=dummy_key_for_ci
git branch --show-current   # claude/work-handover-4rgt1u
git log --oneline -1        # f68516f
git status --short          # 空のはず
```

### 済んだこと（8〜10回目）

| 回 | 反例 | 対処 | commit |
|---|---|---|---|
| 8 | `package-lock.json` が走査からもハッシュ固定からも落ちる | 固定の対象を「全ファイル − 走査したファイル」の**差集合**にした。`--update-baseline` の抜け道も塞ぎ、新規ピンには `--pin "理由"` が要る | `463fc3f` |
| 9 | frontend の外の JS を相対 import／`dist` に `git add -f` | **走査した場所から走査していない場所を名指しできない**という禁止を足した | `097707a` |
| 10 | `'/../x'`／`\u002e\u002e/`／`"file:../x"`／**alias を `path.resolve` で計算** | 前3つは判定を1本にして塞いだ（エスケープを解いてから場所として読む）。**最後の1つは塞げないので『確かめないこと』に落とした** | `f68516f` |

**`/gate-report` はユーザーが打つ。私からは起動しない**（`disable-model-invocation`）。

## 2. P5 の現在地

**ブランチ** `claude/work-handover-4rgt1u` / **HEAD** `f68516f` / **origin/main** `79d595b`
**PR** [#78](https://github.com/kitahara-6355/antigravity-video-studio/pull/78)（open・CI success）は
`cc/p5-closure-gate`（`7639bd4`）を追っている。8〜10回目の修正3本はそこから枝分かれした
`claude/work-handover-4rgt1u` にあり、**#78 には入っていない**。
どちらに寄せるかは判断が出てから決める（内容は #78 の続き）。
#75（正典に P5 を定義）と #76（呼び出し口＋103件の移行）はマージ済み。#77 は #76 に統合して close。

| いまの数字 | 値 |
|---|---|
| 閉包ゲート | 走査 **57 ファイル** / カタログ **84 項目** / 使用 **97 箇所** / 許可 **18 件** / 違反 **0** |
| `ui_api --gate` | matched **88** / 突き合わない **0**（読めなかった 10） |
| `ui_api_ratchet` | 95 → 95 |
| テスト | **335 passed**（閉包 126 / ratchet 45 / ui_api 164）。ruff clean |
| フロント | **`a6817ca` から1ファイルも触っていない**ので数字は不変（`npm run build` 成功 / eslint errors 46 / warnings 6） |

**フルスイートは CI で通っている**（PR #79 / run **31844112319** / Python 3.13）。
マージゲート判定は全条件クリア: **テスト 9,830件中 失敗0 エラー0** /
UX ラチェット失敗0 / カバレッジ **75.9%** / CRITICAL 負債 0件。

なお **Claude Code の実行環境では完走できない**（1プロセスで 13.6GB まで伸びて OOM 137）。
`test_preview_engine.py` は ffmpeg 不在、`test_read_log.py` は `scratch/read_log.py` の
f-string が Python 3.11 で構文エラー（3.12 なら通る）。いずれも環境要因なので、
**手元で完走しないことを異常と読まないこと**。証拠は CI の run ID で取る。

### 検証コマンド（作業の締めに全部）

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
for c in "ui_api --gate" "ui_api_ratchet --ratchet" "ui_api_closure --gate" \
         "ui_api_closure --ratchet" "claim_audit --persona owner --gate" \
         "executor --persona owner --ratchet" "dead_code --ratchet"; do
  out=$(python -m backend.ux_verification.$c 2>&1 | tail -1); echo "[$?] $c → $out"
done
PYTHONPATH=./backend python -m pytest backend/tests/test_ux_ui_api_closure.py \
  backend/tests/test_ux_ui_api.py backend/tests/test_ux_ui_api_ratchet.py -q --no-cov
cd frontend && npm run build && npx eslint . -f json | python -c "..."   # 内訳で main と比較
```

**eslint は総数だけで比べない。** 直った分と増えた分が相殺して同数に見える。
ルール別の内訳で比べる（`no-unused-vars 32 / no-empty 8 / exhaustive-deps 6 /
immutability 3 / set-state-in-effect 2 / no-control-regex 1`）。

---

## 3. 何を作ったか（コードを読む前にこれだけ）

**主題は「検出をやめて閉包にする」。** 散らばった呼び出しの URL を1件ずつ*正しく解決する*のは
精度を要求する仕事で、読み違いが黙って PASS になる（P4 が14回破られた原因）。
**禁止は精度を要求しない** — 誤検出しても FAIL が増えるだけで沈黙にならない。

| 場所 | 役割 |
|---|---|
| `frontend/src/gateway/endpoints.js` | 叩ける先のカタログ。**84項目・1行1項目のリテラル** |
| `frontend/src/gateway/client.js` | 実呼び出し。`apiFetch` / `apiUrl` / `apiSocket` / `openApiUrl` |
| `frontend/src/gateway/external_urls.json` | 外部 URL の許可リスト（18件） |
| `backend/ux_verification/ui_api_closure.py` | 閉包ゲート＋閉包ラチェット（**新規**） |
| `backend/ux_verification/ui_api.py` | P4 のゲート。カタログを一級の呼び出し先として突き合わせる |
| `backend/ux_verification/ui_api_ratchet.py` | P4 のラチェット。`--migrate` / `--resolve` を追加 |
| `.github/workflows/ci.yml` | 214-227 行に閉包の2ステップ（`continue-on-error` 無し） |
| `pytest.ini` | 371-373 行に3テストを登録 |

**メソッドはカタログが決める。** 呼び出し側から指定できない。
ディレクトリ名が `src/api/` ではなく **`src/gateway/`** なのは、`from '../api/client.js'` が
backend プレフィクス `/api/` を含んでしまい、禁止を素のままに保てないから。

---

## 4. **`gate-verifier` に10回破られた。この履歴が一番の資産**

同じ型を繰り返さないために、破られ方の変遷を残す。**2回目以降はすべて「前の修正が作った新しい沈黙」。**

| 回 | 件数 | 破られ方 | 学び |
|---|---|---|---|
| 1 | 4 | 相対 backend パス／`'/'+'api'+'/x'`／`[].join('/')`／変数に束ね直した扉の関数／除外ディレクトリの追加 | 禁止語彙を「呼び出しの形ではない」と外したら、実際に検出が1つ減っていた。**誤検出を消すために禁止を狭めない。コード側を直す** |
| 2 | 3 | `/health`（単一セグメント）／複数行 import／再輸出シム／**テストが CI で走っていなかった** | テストを足したら **testpaths に登録されているか確かめる** |
| 3 | 3 | `frontend/src` の外／`.mts`／`./api/x`／`//host/api/x`／同一キーの重複削除 | 走査範囲を借りると借りた側の限界がそのまま穴になる。**使用は回数まで固定する** |
| 4 | 1 | NUL を1文字混ぜると走査から外れる | **その場しのぎの安全弁を足さない。** 中身で読む／読まないを決める分岐が唯一の fail-open だった |
| 5 | 3 | `src/components/build/` を作るだけ／symlink／`.png?raw`+`new Function` | 除外は**相対パスの完全一致**で。名前で任意階層に効かせない |
| 6 | 1 | `import.meta.glob({as:'raw'})` と素の `Function(` | **綴りの列挙で塞ごうとするな。** 除外の正当性そのものを検証する側に回る |
| 7 | 2 | マジックバイト8バイトの後ろに JS／`CLOSURE_EXCLUDED_FILES` の任意階層一致 | 部分的な検証は必ず抜ける。**中身そのものをハッシュで固定する** |
| 8 | 1 | `package-lock.json` が走査からもハッシュ固定からも落ちる | 走査から外したものは**例外なく**固定する。列挙ではなく**差集合**で決める |
| 9 | 2 | frontend の外の JS を素の相対 import（**難読化なし**）／`dist` に `git add -f` | 走査範囲を「場所」で決めるなら、**外を名指しできないこと**まで含めて初めて閉じる |
| 10 | 3 | `'/../x'`（ルート相対で外へ）／`"file:../x"`／**`resolve.alias` を `path.resolve` で計算** | 判定を2本に分けたら片方だけ緩い（8回目と同型）。**綴りではなく値で見る**（エスケープを解く）。設定で計算されたパスは**字句では閉じない** |

### 繰り返している私の失敗（3型）

1. **修正が新しい沈黙を作る**（2・3・4・5・7回目）。閉包を広げる実装が別の場所を狭める
2. **綴り／部分の列挙で塞いだつもりになる**（1・5・6・7回目）。次の綴りで破られる
3. **測っていないことを『確かめること』に書く**（1・3・4・6・7回目）。
   C-4 が毎回連動して落ちる。**実装を変えたら意味表も同じコミットで直す**

### 4型目になりかけているもの

**「破られた → 塞ぐ」を無限に続ける**こと。10回やって10回破られた。
塞ぐこと自体は正しいが、**条件文が達成不能なら塞いでも終わらない**。
**10回目で (2-B) に当たったので止めた**（§4-B）。

### 効いた原則

- **禁止は精度を要求しない。** コメントも除去しない（コメント内の URL も落とす）
- **判定基準は宣言側から導く**（backend プレフィクスは EndpointRegistry から）
- **正規表現の本体まで**ベースラインに固定する。自分自身の検出器も
- **除外は「読まない場所」ではなく「読まない根拠」を固定する**

---

## 4-B. **止めた理由と、ユーザーに決めてほしいこと**

(2-B) の停止条件のうち「**塞ぐために綴りの列挙を増やすしか手が無い**」に当たった。
残っている反例は1つだけで、それは字句の禁止では原理的に閉じない。

### 残っている反例（10回目の反例2・実測）

```js
// frontend/vite.config.js
import path from 'node:path';
export default defineConfig({
  resolve: { alias: { sharedui: path.resolve(process.cwd(), '..', 'shared_ui') } },
});
```
```jsx
// frontend/src/main.jsx
import { pwn } from 'sharedui/net.js';   // ← frontend の外のコードが動く
```

**リテラルに `../` が1つも現れない。** 使用側は普通のパッケージ名。
4ゲートすべて exit 0 のまま通る。設定は任意のコードなので、
`path.resolve` でも `process.env` でも `[..'.','.'].join('')` でも同じことができる。
**綴りを何個並べても終わらない**（6回目で「誤り」と確認した型そのもの）。

### 1. 反例の分類（8〜10回目）

| 回 | 反例 | 分類 |
|---|---|---|
| 8 | lockfile に JS を埋めて `Blob` + 動的 import | **意図的な多段の回避** |
| 9-1 | frontend の外のモジュールを相対 import | **うっかり**（monorepo の定型。検証者の分類） |
| 9-2 | `dist` に `git add -f` | 意図的（ただし塞ぐのは軽かった） |
| 10-1 | `'/../shared_ui/net.js'` | 単段。私の書き漏らし（判定を2本に分けた） |
| 10-3 | `"file:../shared_ui"`（npm ローカル依存） | **うっかり**（monorepo の定型） |
| 10-2 | **`resolve.alias` を計算で組む** | **うっかり**（monorepo の標準解。レビューで異常に見えない） |

**9・10回目は「多段の意図的な回避」ではない。** だから (2-B) の1つ目の条件では止めていない。
止めたのは2つ目（綴りの列挙しか手が無い）。

### 2. C-1 を達成可能にする案（**どれを採るかはユーザーの判断**）

| 案 | 中身 | コスト | 私の見立て |
|---|---|---|---|
| **A. `limits` に落とす** | 「設定で計算されたモジュール解決は捕まらない」を P4 の `limits` と同じ扱いにする。C-1 は「**うっかり直接呼ぶことを防ぐ**」まで | **ほぼ0**（意味表には既に実測反例つきで書いてある。正典に limits を1項目足すだけ） | **勧める。** P4 で同じ判断を済ませてあり、判定の性質（禁止・fail-closed）も変わらない |
| **B. 宣言の網羅性に変える** | `resolve.alias` の値と `dependencies` をベースラインに固定し、増減を violation にする | 実装 半日 + 検証1周 | alias を1つ足すたびに理由が要る。**正当な変更の摩擦が上がる**が、閉包としては強い |
| **C. 到達可能性に作り替える** | エントリから import グラフを辿り、届く範囲を走査対象にする | 実装1〜2日 + 検証数周 | **勧めない。** 指定子の解決＝精度を要求する判定に戻る。解決に失敗したファイルが黙って読まれなくなる（P4 が14回破られた型） |
| **D. `dist` を検査対象にする** | ビルド成果物を読む | 中（フロントに CI が無い） | 「ビルドが唯一の真実」ではあるが、**CI が無いので手元実行が唯一の証拠**になり、ゲートとして弱い |

### 3. そのまま塞ぎ続けた場合の実測コスト

- 1周（検証＋修正＋再検証）＝ `gate-verifier` **約11万トークン** ×1、私の修正込みで**実時間 30〜60 分**
- 8→9→10 の3周で**破られ方は細くなっている**（8回目=多段の回避 → 10回目=書き漏らし1件＋原理的に閉じない1件）。
  ただし**閉じない1件は周回では減らない**

## 5. 積み残し（P5 の範囲外）

- **`backend/tests/test_ux_snapshot.py` が testpaths 未登録**。登録すると1件落ちる（既存の負債）
- **`backend/tests/test_ux_ratchet.py` は `tests/test_ux_ratchet.py` と同内容の重複**
- `public/assets/samples/*.png` は**拡張子が `.png` なのに中身が JPEG**（閉包の検証で判明）
- P4 の `limits` 4件、D-27 / D-23 / D-24 / D-17、ruff ラチェット −6、Dependabot #67・#15
- `wt-main` worktree（別セッションのスクラッチ。触らない）

## 6. P6・P7（承認済みの順序）

- **P6 — Admin UX 保証**。P1〜P4 の機構を A-* ストーリーに当てる。**新しい機構は作らない**
- **P7 — 実行層（L2）を開ける。有料。** `backend/.env` の設置（ユーザー作業）と Gemini API の予算。
  **設計と金額をセットで見せてから実行**（ユーザー指示）

## 7. ユーザー側の宿題

| 項目 | 状態 |
|---|---|
| Google アカウント `admin@dx-nyasan.com` の閉鎖 | 未着手 |
| `dx-nyasan.com` ドメイン | 失効 2026-09-25。**自動更新解除の期限は 9/9** |

---

## 8. P5 の実現度についての注意

**このフェーズで実現度は上がらない**（65.95% のまま）。`ui_api_connection` は
`scoring_policy` の「静的検証+CI ゲート=最大50%」の上限に P4 で既に達しており、
P5 も静的検証の範囲を出ない。買っているのは点数ではなく **50 の頑健さ**。
正典の `current_phase.score_note` に明記済み。ゲート報告でもそう伝えること。
