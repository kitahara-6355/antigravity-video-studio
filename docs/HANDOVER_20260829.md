# 引継ぎ 2026-08-29 — R1.5 の C4・C6 検証（5周目の続き）

**読むのはこの1枚だけ。** `CLAUDE.md` と SessionStart フック（ロードマップ）は自動で入る。
`docs/HANDOVER_20260828.md` 以前は**経緯の保管庫。開かなくていい**。

---

## 0. 3行で

- **C5 は met。C4 と C6 は未達のまま。** C4 は **5周**、C6 は **3周**の検証を使っている
- **やることは2つだけ**: ①C4 の6周目を回す ②C6 を再検証する（3周目の met は古い版のもの）
- **`/gate-report` はまだ実行しない。** 両方 met になり、正典に立ててからユーザーに依頼する

## 1. 手元の状態

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
git log --oneline -1     # 08dfcff
git status --short       # 空のはず
```

| | |
|---|---|
| main | `8eef716` |
| 作業ブランチ | `cc/r15-mainline` / HEAD `08dfcff` / PR **#81**（draft）|
| **CI** | `08dfcff` の run を確認すること（`gh run list --branch cc/r15-mainline --limit 1`）。直前の緑は run **33167760808**（`5e3eed0`・10,204件 失敗0 / カバレッジ 76.0%）|
| 予算 | `R1.5-mainline` **¥87.94 / ¥1,000**（無料枠なので実費は ¥0）|

- **`backend/.env` に実キー。触らない・読まない**
- **CLI は `PYTHONPATH=./backend` と `GOOGLE_API_KEY=dummy_key_for_ci` が要る**
- 実行記録は `output/runs/` に2件ある（`.gitignore` 対象＝この端末にしか無い）

## 2. R1.5 の現在地

| 条件 | met | 状態 |
|---|:--:|---|
| C1a〜C1d・C2 | ✅ | 承認・マージ済み |
| C3 | ✅ | 2周で met |
| **C4** | ❌ | **5周目 not_met。指摘は対応済み・未検証**（§3）|
| C5 | ✅ | 正典2箇所に met を立て済み |
| **C6** | ❌ | **3周目 met だが判定が `7a7957a` 時点で古い。再検証が要る**（§4）|

## 3. C4 — 6周目に何を見せるか

### 5周目の指摘と対応（すべて `08dfcff` までに対応済み）

| # | 指摘 | 対応 |
|---|---|---|
| **C-6**（決定的）| **私が5周かけて直していた `/api/review/*` は本番にマウントされていない**（実測でルート数 0）。本番で生きている品質スコアは `/api/pipeline/quality-gate/*` で、動画を見ずに 85 点と現在時刻を返し、UI が「QUALITY SCORE」として表示していた | **2026-08-28 ユーザー決定で C4 の範囲に含めて修正。** quality-gate 系6経路に `QUALITY_GATE_DATA_SOURCE`（`is_real: false`）を付け、`checked_at` に現在時刻を打つのをやめた。台帳に `pipeline_quality_gate_ui` を登録 |
| **C-3** | guard の `_参照形()` が `patch("model_registry.get_model")` 形をトップレベルモジュールで拾えず、16ファイルが対象外 | `"{mod}.` / `'{mod}.` を照合に追加。対象 142 → **158 ファイル** |
| **C-8** | guard が `MAX_FILES` 超過とタイムアウトのどちらでも exit 0 | どちらも **exit 1**。`MAX_FILES` を 160 → 400 |

### 6周目の検証で必ず伝えること

- **`review_router` は死蔵のまま残すとユーザーが決めた**（2026-08-28）。
  リポジトリ自身の死蔵コードラチェットが「本番では 7本すべて 404」と記録している。
  **未達根拠にしないよう明記する**
- **`progressive_review` の統一感スコア**（1つも測れていなくても 100.0）は
  **R2 送りとユーザーが決定**。正典 `R1.5` の `limits` に記録済み。**未達根拠にしない**
- **`decision` は訂正済み** — `upload_video()` は例外ではなく
  `UploadResult(success=False, error="not_implemented")` を返す
- **元から赤いテストは実測で確定している**（`8eef716` と突き合わせて 38 ファイル）
- **CI run ID は必ず `gh run view <ID>` で実在を確認してから渡す**（4周目で誤記した）

### 5周連続で落ちた理由（同じ轍を踏まないために）

**毎回「同じクラスの別経路」を見落としていた。**

```
1周目 N-4  tests/test_youtube_uploader_service.py     testpaths 外
2周目 C-3  test_shared/test_cov_admin_channel_router.py  testpaths 外
3周目 gap  test_shared/test_report_generator_plugin_robustness.py  testpaths 外
4周目 C-6  admin_analytics_router.py                  1ファイル隣
5周目 C-6  pipeline_router の quality-gate            **直した方が死蔵だった**
```

対策として `.github/scripts/outside_testpaths_guard.py` と CI ジョブを入れたが、
**guard を強くするたびに新しい退行が見つかっている**（16 → 17 → 18件）。
6周目でも「まだ尽きていない可能性」を前提に、**網羅的に探すよう指示すること。**

## 4. C6 — 再検証が要る

3周目で **met**（変異テスト5脚すべて、到達可能性の独立再実装、残存31箇所の全件手検分）。
**ただし判定は `7a7957a` 時点**で、その後こう動いている:

- `model_policy.py` に `default_model()` を追加
- `test_batch21_deep_coverage.py` の C6 起因の退行を修正（38件連鎖 → 90 passed）
- 17件目・18件目の退行修正（`test_cov_phase5_b_class_1.py` ほか）

**`--sunset` は手元で exit 0 のまま**だが、判定の前提が変わっているので回し直す。
3周目の指摘（`sunset_gate` が段と実行記録を見ていない等）は全部塞いである。

## 5. 検証コマンド

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --gate          # C1c
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --ai-effect     # C3
PYTHONPATH=./backend python -m backend.model_policy --sunset                 # C6
PYTHONPATH=./backend python -m backend.feature_gaps --audit --static-only
python .github/scripts/ruff_ratchet.py
python .github/scripts/outside_testpaths_guard.py --base main --list         # 対象を見るだけ
```

**テストは1ファイルずつ。** ファイル間に順序依存があり、まとめると別の場所で落ちる。

## 6. 踏んだ罠（このセッションで実際にやらかしたもの）

- **`git add -A` がテスト実行の書き換えを拾う。** `backend/branding/design_tokens_history.json`
  の追記211行を誤ってコミットした（`5e3eed0` で撤回）。**テスト実行後は `git status` を1件ずつ見る**
- **CI run ID を誤記した**（`33154713247` は存在しない）。証拠を渡す前に `gh run view` で確かめる
- **死蔵コードを5周かけて直した。** `backend/ux_verification/dead_code.py` が
  既に記録していた。**直す前に「その経路は本番で生きているか」を確かめる**
  （`main.py` の `include_router` を見る / TestClient でルート数を数える）
- **gate-verifier を2つ並走させると互いの測定を汚す**（変異テストが衝突する）。**直列で回す**
- **Bash ツールのヒアドキュメントはバックスラッシュを壊す。** Python スクリプトに
  `\` を含めるときは `chr(92)` を使うか、行番号スライスで置換する
- **Python の `open(..., 'w')` は Windows で `\n` を `\r\n` にする。**
  シェルに食わせるファイルを書くときは `newline='\n'` を明示する
- **手元（Windows）でタイムアウトするテストがある**（`test_cov_phase5_b_class_1.py`）。
  CI（Linux）では走る。**「判定不能」を「問題なし」と読まない**

### 元から赤いテスト（`8eef716` と突き合わせて確定・38 ファイル）

`test_harness_auditor.py`(7) / `test_analyst.py`(14) / `test_governance.py`(13) /
`test_batch8_pipeline_asset_director.py`(20) / `test_websocket.py`(8+4errors) /
`test_gemini_client_factory.py`(4) / `test_fitness_functions.py`(4) /
`test_stage_bound_agent.py`(3) / `test_telop_proposal_engine.py`(7) ほか。
**個別に確かめるときは `git worktree add --detach <path> 8eef716` で基準を展開して比べる。**

## 7. このセッションで作ったもの（場所だけ）

- `.github/scripts/outside_testpaths_guard.py` + CI ジョブ `testpaths 外の退行検知` —
  **testpaths の外にある 443 ファイルを CI が一度も見ていなかった**問題への対策。
  基準の版を `git worktree` に展開して失敗テスト名を突き合わせる（ベースラインを固定しない）
- `backend/model_policy.default_model()` — 既定モデルの読み口
- `docs/C4_DEPENDENCY_RUN_20260828.md` — C4 の verify（1〜5周目の記録）
- `docs/C6_SUNSET_20260828.md` — C6 の verify（1〜3周目の記録）

## 8. 進め方の約束

- **`met` は自分で立てない。** `gate-verifier` に別コンテキストで検証させてから。
  正典の `met` は **2箇所**（`current_phase` と `roadmap.phases["R1.5"]`）
- **gate-verifier は直列で回す**（並走させない）
- **`verification_policy` の2周上限はとうに超えている。** ユーザーが周回を承認済み。
  ただし**周回のたびに範囲の判断をユーザーに上げる**こと（5周目は
  「品質スコアはデモ層を含むか」を上げて「含める」と決まった）
- テストが3回連続で FAIL したら止めて報告する
- 有料利用は予算の範囲内なら都度確認不要（憲法第3条）。pro 昇格は別途承認
- **C4・C6 が揃ったら R1.5 のフェーズ終了。** ユーザーに `/gate-report` の実行を依頼して止まる
