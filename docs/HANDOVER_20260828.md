# 引継ぎ 2026-08-28 — R1.5 後半（C4・C6 の修正から）

**読むのはこの1枚だけ。** `CLAUDE.md` と SessionStart フック（ロードマップ）は自動で入る。
`docs/HANDOVER_20260827.md` 以前は**経緯の保管庫。開かなくていい**。

---

## 0. 素材の件（**対応済み。確認だけでよい**）

C5 用の 30 分素材は**ユーザーが置き直し済み**。復元を実測で確認した（2026-08-28）:

```
output/testclips/2025-09-22_16-34-33.mp4
  1,798.166 秒（30.0 分） / 600,559,949 bytes   ← 消える前と完全に同一
```

経緯: テストの autouse フィクスチャが `shutil.rmtree(Path("output"))` を実行しており、
リポジトリ直下の `output/` が丸ごと消えた（**修正済み** `60db9fa`。削除範囲を
`output/generated/` に限定）。**同じ事故はもう起きない。**

まだ戻っていないもの:

- **`output/runs/` の実行記録 18 件**（消失）。このため `artifact_gate --gate` と
  `--ai-effect` が **exit 1**。壊れているのではない。**実走を1本すれば戻る**
- `output/testclips/r1_clip30s.mp4`（30秒クリップ）。
  `pipeline_work/job_20327f284f79/r1_clip30s_normalized.mp4`（3.7MB）に
  正規化済みコピーが残っており、実走の入力に使える
- C5 の判定には影響しない（数字は `.claude/cost_ledger.jsonl` /
  `backend/data/sessions/` / `vault-outputs/*.quality.json` の3系統で裏が取れており、
  gate-verifier が突き合わせ済み）

---

## 1. 手元の状態

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
git log --oneline -1        # 60db9fa
git status --short          # 空のはず
```

| | |
|---|---|
| main | `8eef716`（中間ゲート C1a〜C1d・C2 は承認・マージ済み）|
| 作業ブランチ | `cc/r15-mainline` / HEAD `60db9fa` / PR **#81**（draft）|
| **CI** | 直近の緑は run **33112526069**（`3edcef1`・テスト **10,178件 失敗0** / カバレッジ 75.8% / 汚染 0）。`60db9fa` は未確認 |
| 予算 | `R1.5-mainline` **¥82.91 / ¥1,000**（無料枠なので実費は ¥0）|

- **`backend/.env` に実キー。触らない・読まない**
- **CLI は `PYTHONPATH=./backend` が要る**
- **`artifact_gate --gate` はいま exit 1**（実行記録 0 件）。壊れているのではなく、
  上記の消失が原因。**実走を1本すれば戻る**

## 2. R1.5 の現在地

| 条件 | met | 状態 |
|---|:--:|---|
| C1a 実行記録が再開材料になっている | ✅ | 承認・マージ済み |
| C1b 動かなかった工程を成功に数えない | ✅ | 同上 |
| C1c ゲートが赤い実走を FAIL にする | ✅ | 同上 |
| C1d 本線が agents に一本化 | ✅ | 同上 |
| C2 モデル解決器が1つ | ✅ | 同上 |
| C3 AI の出力が成果物に届く | ✅ | 2周かかって met |
| **C4 偽の success を止める** | ❌ | **1周目 not_met。指摘4件（§3）** |
| **C5 原価と所要時間の実測** | ⬜ | **検証は met。正典の met を立てるだけ**（§4）|
| **C6 2.5系サンセット** | ❌ | **1周目 not_met。指摘2件（§5）** |

**周回**: C4 は残り1周、C6 は残り1周、C5 は残り1周。3周目は無い（`verification_policy`）。

## 3. C4 の指摘4件（gate-verifier 1周目・すべて「うっかり型」）

**①〜④を1つずつ実際に動かした記録は `docs/C4_DEPENDENCY_RUN_20260828.md` にある**（これが verify）。
偽の success を止めたつもりが、**別経路で残っていた**。

| # | 場所 | 中身 |
|---|---|---|
| **N-1**（決定的）| `backend/routers/youtube_optimizer.py:1393-1440` | `retention_map_plugin.analyze_retention_risks()` を **`IMPLEMENTED` を見ずに直接呼び**、`:1435` で `"success": True` を返す。さらに `preview_report_generator.generate_html_report()` で **HTML 成果物まで書く**。同じリクエストで値が毎回変わる（乱数がそのまま API 応答と成果物に載る）。**この挙動を `backend/tests/test_youtube_optimizer_router.py:645 test_retention_map_success` が `assert ...["success"] is True` で固定しており、CI で緑**。塞ぐときはこのテストも新挙動へ |
| **N-2** | `backend/plugins/progressive_review_plugin.py:348-353` | `context.quality_score or 0` → `f"品質スコア: {quality_score:.1f}/100"`, `passed = quality_score >= 90`。**`report_generator_plugin` で直したのと同じ経路**（`backend/core/context.py:67` の既定値 0.0）が未修正。未計測が「0.0点・不合格」という測定結果として出る。`backend/routers/review_router.py:131` から公開 |
| **N-3** | `backend/routers/admin_channel_router.py` | `DATA_SOURCE` を混ぜたのは 20 GET 中 **3本だけ**。残り17本が無印で固定値を返す。特に **`:478-488 get_youtube_connection` が `connected: true` と現在時刻の `last_sync` を返す** — 同ファイル60行上の `DATA_SOURCE` が「接続していません」と書いているのに。`get_growth_prediction` は `"model": "linear_regression_v2"` と存在しない推論を自称。**②の契約を固定するテストは1件も無い**（`grep -rn "is_real\|DATA_SOURCE" backend/tests/` = 0件）ので1件足す |
| **N-4** | `tests/test_youtube_uploader_service.py:322-325` | `assert result.success is True` / `video_id == "placeholder_video_id"` を保持したまま**赤になっている**。起点 `8eef716` では緑だった＝**この変更が赤くした**。`pytest.ini` の testpaths 外なので CI は見ていない。`backend/tests/test_youtube_upload.py` の重複テストで、片方だけ直した取りこぼし |
| **N-5**（記録の誤り）| `docs/C4_DEPENDENCY_RUN_20260828.md:94-95` | 「①②④ の契約はテストで固定してある」が誤り。実際は **①③④**。②を固定するテストは無い。③が引く `run.json` は消失しているので、裏取り先を `vault-outputs/final/final_20260828_012707.quality.json` に書き換える |

**弱化は無かった**（検証者が明言）。更新した2件はどちらも正しい方向。

## 4. C5 は met。正典を立てるだけ

gate-verifier が全項目を記録と突き合わせて **met**。所要・原価・呼び出し数・状態・品質スコア・
工程ごとの内訳すべて一致。RPD/RPM も検証者が再計算して一致。

```
run 20260827T184459832485-0000 / completed / 1,199.8秒(20.0分) / ¥42.69 / AI 6回 / 品質94点
RPD 1,000 ÷ 6 = 166本  |  RPM 実測ピーク 2.42回/分 vs 上限15 → 非拘束  |  実時間 24h ÷ 20分 = 72本
→ 効いている制約は実時間。無料枠には収まる。
```

**やること**: `backend/branding/vision_backlog.json` の `R1.5-C5` の `met` を
**2箇所**（`current_phase` と `roadmap.phases["R1.5"]`）で `true` にする。
あわせて `docs/C5_COST_MEASUREMENT_20260828.md` に「`run.json` は消失。裏取りは
ledger / sessions / traces / quality.json」を追記する（検証者の申し送り）。

## 5. C6 の指摘2件（gate-verifier 1周目・「うっかり型」）

**私の到達可能性判定に穴があり、「本番 0 箇所」は嘘だった。**

| # | 場所 | 中身 |
|---|---|---|
| **N-1**（致命）| `backend/model_policy.py:575` | `elif isinstance(node, ast.ImportFrom) and node.module and node.level == 0:` — **相対 import を辿らない**。`backend/routers/__init__.py` は全行が相対 import で、`backend/main.py:167` が `from routers import (...)` で実行する。結果 `routers/**` 全体が「到達不能」に落ちていた |
| **N-2**（致命）| `backend/model_policy.py:573-576` | `from pkg import sub` で `pkg` しか積まない。`f"{module}.{name}"` も積む必要がある |
| **N-3** | `backend/model_policy.py:514` | `rglob("*.py")` のみで**設定データを走査していない**。`backend/model_config.json:311-320` の `deprecated` が実行時に 2.5 を返す（実測: `gemini-2.0-flash-lite → gemini-2.5-flash-lite`、`gemini-2.5-pro → gemini-2.5-flash`）|

**直せば現れる本番の 2.5 参照（検証者が実測した5箇所）**:

```
backend/routers/websocket.py:12              def get_model(task): return "gemini-2.5-flash"
backend/routers/admin_setup_router.py:279    "primary": {"tier": "premium", "model": "gemini-2.5-pro"}  ← API が返す値
backend/live_api_handler.py:12               同じフォールバック
backend/services/hook_improver.py:18         同じフォールバック
backend/usage_tracker/quota_manager.py:25    同じフォールバック
```

**順序**: 先に走査器を直して**赤を見せてから**潰す。それがゲートが空振りでない証拠になる。
`admin_setup_router.py:279` は定数ではなく `model_config.json` から読む形にする（同じ腐り方の再発防止）。

**もう1つ**: 条件文は「実行記録にも 2.5 が無いこと」を求めるが、いま記録が 0 件で
`--sunset` は「⚠ 確かめられていません」と出す。**実走を1本すれば埋まる。**

## 6. 踏んだ罠（同じ所で止まらないために）

- **テストが本番の `output/` を丸ごと消していた**（§0）。修正済みだが、**同種の
  `rmtree` / 相対パス書き込みが他にも無いか**は未点検
- **テストファイルをまとめて走らせない。** ファイル間の順序依存があり、
  実測で**8ファイルまとめて33件失敗 → 1ファイルずつなら全部緑**。CI はバッチが固定なので
  CI の結果を正とする
- **`git checkout -- backend/usage_tracker/` は `tracker.py` も巻き戻す。**
  実行時状態の JSON を戻すつもりでソース修正を消した（実際にやらかした）
- **`pytest.ini` の testpaths を触らない。** バッチの区切りが変わり、既存の汚染が別の場所で発火する。
  **新規テストファイルを作らず、testpaths 内の既存ファイルに足す**
- **`routers` を import すると本番ディレクトリ（`output/generated` 等）が作られる。**
  テストに書くと汚染ラチェットに乗る
- **連続 push すると CI が前の run を cancel する。**「判定不能（test-results.xml 解析失敗）」は
  cancel された run の残骸で、失敗ではない
- **フルスイートはローカルで OOM**（約13.6GB）。必ずファイル指定
- **実走は課金経路。** `AVS_SKIP_LEARNING_SIDE_EFFECTS=1` を付けないと DreamEngine が
  追跡ファイルを書き換える

### 元から赤いテスト（`8eef716` でも赤い。退行ではない）

`test_harness_auditor.py`(7) / `test_e2e_pipeline.py::TestShortsGeneratorUI::test_shorts_generator_has_render_button` /
`tests/test_model_governance.py::test_resolve_model_edge_cases` / `test_gemini_client_factory.py` /
`test_stage_bound_agent.py` / `test_fitness_functions.py` / `test_quality_audit_fixes.py` /
`test_workers/test_youtube_opt_worker.py`(6) / `test_workers/test_quality_gate_worker.py`(2) /
`test_shared/test_cov_*` / `test_api_contract.py`(2)

**`tests/test_youtube_uploader_service.py` は元は緑**（C4 の N-4）。

## 7. 検証コマンド

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
PYTHONPATH=./backend python -m pytest backend/tests/test_revenue_artifact_gate.py -q --no-cov
PYTHONPATH=./backend python -m pytest backend/tests/test_agents_run_record.py -q --no-cov
PYTHONPATH=./backend python -m pytest backend/tests/test_model_sunset.py -q --no-cov
# ↑ 1ファイルずつ
```

```bash
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --gate          # C1a（いま exit 1）
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --ai-effect     # C3
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --intermediates # C3
PYTHONPATH=./backend python -m backend.model_policy --audit                  # C2（実キー）
PYTHONPATH=./backend python -m backend.model_policy --sunset                 # C6
PYTHONPATH=./backend python -m backend.feature_gaps --audit                  # 実装不足の台帳
PYTHONPATH=./backend python -m backend.cost_guard --status                   # C5・残高
python .github/scripts/ruff_ratchet.py && python .github/scripts/fs_guard_ratchet.py
```

## 8. このセッションで作ったもの（場所だけ）

- `backend/config/feature_gaps.json` + `backend/feature_gaps.py` — **実装不足項目の台帳と点検**。
  台帳に載っている機能は品質ゲートが減点しない（実装したら台帳から消え、その瞬間から見はじめる）。
  設計 `docs/specs/2026-08-27-feature-gaps-design.md` / 計画 `docs/plans/2026-08-27-feature-gaps.md`
- `docs/C4_DEPENDENCY_RUN_20260828.md` — C4 の verify（4件の実走記録）
- `docs/C5_COST_MEASUREMENT_20260828.md` — C5 の verify（30分素材の実測）

## 9. 進め方の約束

- **進め方の判断は確認不要。事後報告でよい**
- **`met` は自分で立てない。** `gate-verifier` に別コンテキストで検証させてから。
  正典の `met` は **2箇所**（`current_phase` と `roadmap.phases["R1.5"]`）で立てる
- 同一条件への `gate-verifier` は**最大2周**。3周目の前に条件文を見直してユーザーに上げる
- テストが3回連続で FAIL したら止めて報告する
- 有料利用は予算の範囲内なら都度確認不要（憲法第3条）。pro 昇格は別途承認
- **自分の実装にゲートを効かせて確かめる。** 恒真の判定式を1度書いた（C3 1周目）。
  変異テスト（実装を1つ戻してテストが落ちるか）で2度助かっている
- C4・C5・C6 が揃ったら **R1.5 のフェーズ終了**。`/gate-report` を出して止まる
