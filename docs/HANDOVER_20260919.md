# 引継ぎ — R2「承認工程を必須にする」の着手（作成 2026-09-19）

**この文書だけ読めば続きができるように書いてある。** R1.5 までの経緯は `docs/HANDOVER_20260918.md`（保管庫）。

## 0. 3行で

- **R1.5 は 2026-09-19 にユーザー承認で閉じた**（10/10 met・gate-verifier 通算29周）。正典の `phase_history` にある
- **再監査を済ませた**（58.55%・`docs/vision_audit_20260919.md`）。下げ幅 −7.40 のうち −7.75 は方針の適用で、退行ではない
- **現フェーズは R2（着手済み）。** 終了条件は R2-C1・C2・C3・C5 の4つ（C4 は R3-C5 へ移した）。予算 ¥1,000（`R2-approval`）

## 1. 手元の状態（書いた時点）

| 項目 | 値 |
|---|---|
| main | PR #81（R1.5）を fast-forward でマージする予定（CI run 35409157620 の完走待ち） |
| 次のブランチ | `cc/r2-approval`（再監査と R2 の定義のコミットから始める） |
| 正典 | `backend/branding/vision_backlog.json` の `current_phase` = R2（`status: in_progress`・`axis: ux_guarantee_owner`） |
| 予算 | `.claude/budget.json` の `R2-approval`（¥1,000・active）。R1.5-mainline は closed（実績 ¥92.84） |

## 2. R2 の終了条件（2026-09-19 のユーザー決定を反映済み）

| ID | 条件（要約） | verify |
|---|---|---|
| R2-C1 | 承認していない動画は書き出せない（承認フラグが無いと exit 1）。**承認の対象は品質改善ループを終えた後の動画**（D-41） | `python -m backend.revenue.approval_gate --gate` |
| R2-C2 | 承認の証跡が動画ごとに残る（いつ・どの案を・どう直して。人が手を入れた差分） | `python -m backend.revenue.approval_gate --trace <video_id>` |
| R2-C3 | AI 生成の開示に必要なメタデータが出力に含まれ、欠けていたら書き出しが止まる | `python -m backend.revenue.approval_gate --gate` |
| R2-C5 | 承認画面でどのモデルの提案かが見え、その場で昇格できる。**なぜそのモデルになったか（降格の理由）も見える**（D-39） | `python -m backend.model_policy --show` かつ run.json の降格の理由 |

**R2 の前提（未検証）**: D-16 / D-07（Progressive Preview 承認フロー）の未実装部分が、承認工程の土台として使える。
**着手時に1回動かして確かめる**（正典の `roadmap.phases.R2.assumptions`）。

R2 で扱う積み残し（条件ではない）: D-43（改善ループが不合格で終わると工程表示が running のまま）・
D-28（C4b の門は90点の閾値から作る表示を見ていない）。

## 3. 最初にやること

1. **#81 のマージを確かめる**（`git log origin/main -1` が R1.5 の最後のコミットか）。まだなら CI を見て fast-forward
2. **R2 の前提を確かめる** — D-16 / D-07 の現状を読み、承認フローの土台として使えるかを実物で見る
3. **R2 の設計と計画を書く**（`docs/specs/`・`docs/plans/`）。D-41（改善ループを書き出しの前に）と R2-C1（承認なしで書き出さない）は
   書き出しの流れを一緒に組み替える作業になる
4. TDD で1条件ずつ。**met は自分で立てない** — gate-verifier に別コンテキストで判定させる

## 4. 今日わかった罠（**必ず読む**）

- **テストが本番の出力を汚す・消す事故が3回起きている**（D-29）。今日の2件は直した（`3490468`）:
  `tests/conftest.py` に実行記録の隔離を足した／`test_dm_04` が本物の `vault-outputs/preview`・`merged` を消していた。
  **新しいテストで出力を扱うときは `tmp_path` と `AVS_RUNS_DIR` を使う**
- **実行時に書き換わる追跡ファイルはコミットしない。** 実走のあとに `backend/usage_tracker/` の2ファイルや、
  学習の記憶（`backend/agents/memory/` の3つ・`backend/branding/decision_log.json`）が変わる。
  `usage_tracker` を入れると **C4b の門がその日だけ落ちる**（D-35）。費用の台帳（`.claude/cost_ledger.jsonl`・
  `.claude/budget.json`）だけはコミットする
- **学習を有効にして実走すると、検証済みの事実が消えることがある**（D-44・10件 → 0件）。実走は
  `AVS_SKIP_LEARNING_SIDE_EFFECTS=1` で走らせ、学習を測るときだけ外す（外したら記憶ファイルを戻す）
- **文字起こしはキャッシュを使う。** 素材の隣の `_whisper_<hash>.jsonl` が 1,000 バイトを超えていると Whisper を飛ばす。
  厳格に測るときは退避してから走らせる
- **画面を見るときはバックエンドをダミーキーで起動する。** リポジトリの直下から
  `GOOGLE_API_KEY=dummy_key_for_ci python -m uvicorn main:app --app-dir backend --host 127.0.0.1 --port 8000`
  （`backend/` の中から起動すると `backend.services` を import できずに落ちる）。フロントは `cd frontend && npm run dev`。
  止めたあと **vite の node プロセスが 5173 に残る**ので、`netstat -ano` で確かめて止める
- **正典の写しは全部揃える。** `current_phase` と `roadmap.phases[現フェーズ]` の両方にある鍵（`condition_notes` を含む）は
  同じ値にする。正典の契約テストが見る
- 前回の罠（テストが他の pytest を殺す・pytest-randomly・ヒアドキュメントのバックスラッシュ・RUF059 など）は
  `docs/HANDOVER_20260918.md` §6

## 5. 進め方の約束（ユーザーとの取り決め）

- **エスカレは選択肢つきで。** 表（判断・発見の大きさ・コスト・推奨）→ `AskUserQuestion`。依存する判断は後の段へ
- 範囲の判断（対象か limits か）と周回を止める／続ける判断はエスカレする。進め方の判断は確認不要
- `gate-verifier` 1周は **23万〜47万トークン・6〜36分**（29周目は7条件で約47万・約33分）
- 契約テストを書いたら必ず変異を当てる。push は CI の run が完走してから（testpaths 外の検知は約1時間50分）
- 自分の修正が別の条件を壊していないかを、直したその場で確かめる（今日も2回、自分の変更で C4b の門や正典の契約テストを落としかけた）

## 6. 次セッションで貼るプロンプト

```
R2（承認工程を必須にする）に着手したところです。docs/HANDOVER_20260919.md だけ読んで、§3 の最初にやることから続けてください。
```
