# 引継ぎ 2026-08-19 — R1 実走中（**古い。経緯の保管庫**）

> **これは古い引継ぎです。現行は `docs/HANDOVER_20260826.md`。**
> R1 はその後 7/7 met で完了しました。以下は当時の状況の記録で、
> ここに書かれた「いますぐやること」は**すべて完了済み**です。

**読むのはこの1枚だけ。** `CLAUDE.md` と SessionStart フック（ロードマップ）は自動で入る。
`docs/HANDOVER_20260816.md` は**経緯の保管庫。開かなくていい**（決着済みの議論が大量にある）。

---

## 0. 手元の状態（確認だけ・変更不要）

```bash
cd /c/Users/PC_User/Desktop/script/antigravity-video-studio
git branch --show-current    # claude/work-handover-4rgt1u
git log --oneline -1         # 6520f77 以降
git status --short           # 空のはず
```

- **`backend/.env` に実キーが入っている**（`avs-prod-free` / 個人 / **無料枠**）。**触らない・読まない**
- **CLI は `PYTHONPATH=./backend` が要る**（裸 import が50箇所超の慣習。付けないと落ちる）
- **別セッションが同じブランチに push することがある。** push 前に `git fetch` して rebase する

---

## 1. いますぐやること

### (1) 429/503 に指数バックオフを入れる ← **ここから**

実走で `soul_feedback` が **503 UNAVAILABLE**（`gemini-3.7-flash` が混雑）を受け、
**即座にスタブへフォールバックした。リトライしていない。**

一次情報の指示は「短時間待って再試行」だけ。そして**これは BAN 回避の唯一の実装要件**でもある
（リトライ連打は自動化された乱用と区別がつかない）。429 と 503 の両方に効かせる。

入れる場所は `backend/video_pipeline/soul_feedback_engine.py` の
`_call_llm_for_suggestions`。ただし**同種の呼び出しは他工程にもある**ので、
`gemini_client_factory` / `model_governance` の層に寄せられないか先に見ること。

### (2) 原価を実測する（**R1-C2**）

(1) が入れば Gemini 呼び出しが成功する見込み。そこで:

```bash
export GOOGLE_API_KEY=$(grep '^GOOGLE_API_KEY=' backend/.env | head -1 | cut -d= -f2-)
PYTHONPATH=. python -m backend.video_pipeline.pipeline_coordinator output/testclips/r1_clip30s.mp4
PYTHONPATH=./backend python -m backend.cost_guard --status
```

**いま台帳にあるのは `--probe` の1件だけ。**パイプラインの呼び出しは全部失敗している。
成功した呼び出しが載ったら `.claude/budget.json` の `spent_jpy` を更新する。

素材 `output/testclips/r1_clip30s.mp4` は生成済み（シーン03 から30秒・実写の対談）。

### (3) `RunRecorder` を各工程に被せる（**R1-C4/C5/C6**）

書き方は `backend/revenue/run_record.py` の docstring と
`python -m backend.revenue.artifact_gate --semantics` を見る。
**包まないと `model_not_recorded` で FAIL する（それが仕様）。**

---

## 2. R1 の現在地

| 条件 | 状態 |
|---|---|
| C1 再生可能な mp4 | **実体はできた**（29.53秒・映像+音声・字幕焼き込み済み）が**実行記録が無い**ので未達 |
| C2 実コストと所要時間 | ⬜ **(1)(2) で取る** |
| C3 上限で止まる | ✅ 2026-08-15 |
| C4 失敗工程から再開 | ⬜ 仕組みは完成・未装着 |
| C5 使ったモデル名が残る | ⬜ 同上 |
| C6 工程ごとのモデルが残る | ⬜ 同上 |
| C7 段が一次情報と突き合わせ済み | **✅ 2026-08-19**。4段すべて実 API の models.list に実在 |

**`met` は自分で立てない。`/gate-report` はユーザーが打つ。こちらから起動しない。**

所要時間の実測: 30秒素材で **10/10 完走・53〜62秒**（transcribe 14〜21秒 / soul_feedback 16秒）。

---

## 3. 今日踏んだ罠（同じ所で止まらないために）

### **完走したら成果物のハッシュを入力と比べる**

1回目の実走は「10/10 完走・品質ゲート 89.2点 PASS」で終わったのに、
**成果物が入力と SHA256 まで一致していた**（字幕が1文字も焼かれていない）。
このパイプラインは「何もしなかった」を成功として返す箇所がまだある可能性がある。

```bash
sha256sum pipeline_work/job_*/smartcut_output.mp4 pipeline_work/job_*/composed_output.mp4
```

**一致していたら compose が仕事をしていない。**

### 直した2件（再発防止テスト済み）

| コミット | 内容 |
|---|---|
| `e78f686` | `-vf subtitles=` で Windows のパス区切りがエスケープに食われる。**CI(Linux) では原理的に再現しない**。失敗を二重に握り潰して `success=True` を返していた |
| `6520f77` | `soul_feedback` がモデルを直書きし、フォールバック連鎖で 404。**`--show` の表示と実際に呼ぶモデルが食い違っていた** |

### 既知の負債（R1 の合否には効かないが成果物の質に効く）

- `ComposeResult.duration` が **5.0 直書き**（29.53秒の動画に 5.0 を返す）
- **MeCab 辞書が無く**、日本語解析が毎回 正規表現にフォールバック（字幕の行分割が劣化）
- Whisper が `base` 固定で日本語の固有名詞が崩れる。旧ツリーの**固有名詞辞書139件はこの経路に入っていない**
- `backend/tests/test_shared/test_cov_gemini_client_factory.py` と
  `backend/tests/test_quality_audit_fixes.py` に **HEAD 時点で5件の失敗**。
  どちらも **testpaths 外なので CI は緑**。私の変更が原因ではない
- **二重モジュール問題**: `cost_guard` と `backend.cost_guard` が別オブジェクトになりうる。
  課金台帳が分裂する危険。**部分的な import 修正は destabilize するので手を出さない**
  （一度試して全部戻した）

---

## 4. アカウントとキー（**決着済み・蒸し返さない**）

構成の正典は `CLAUDE.md` の「API プロジェクトとキーの管理」。要点だけ:

| 役割 | プロジェクト | アカウント | 請求先 | 状態 |
|---|---|---|---|---|
| **通常運転** | `avs-prod-free` | 個人 | **なし** | **✅ 稼働中。`.env` に入っている** |
| **昇格専用**（pro） | `avs-prod-paid` | 法人 | あり | 請求先リンク済み。**前払いクレジット未購入なので 429** |

- **法人では無料枠が構造的に使えない**（請求先なし→403 / あり→前払いが要る）。実測済み
- **`pro` 昇格が必要になるまでクレジットは買わない**（1年で失効・払い戻し不可・最低 ¥2,000）
- ユーザー決定（全承認期間）: **1人体制は無料枠を最大限活用。対談相手の了承取得済み。
  引かれた線は『BAN・パージされない限り』の1本だけ**
- **無料枠最大化は通常運転の話。`pro` 昇格（課金）は否定していない。**
  P3 の選定基準は不変。¥3,000 は焼き直し用に温存（**未使用**）

### ユーザー側の残件（催促しない）

- 法人の古いキー2本（`...4LVQ` / `...my7A`）を削除し、`avs-prod-paid` で1本作り直す
- Spend Cap の設定（金額を聞いたら `.claude/budget.json` に記録する）
- 個人の請求先 `01B675-738297-BB2BD0` の前払い移行。
  **`avs-prod-free` は請求先を外したので影響を受けない**

---

## 5. 進め方の約束

- **進め方の判断は確認不要。事後報告でよい**
- **`/gate-report` はユーザーが打つ。こちらから起動しない**
- 予算 `R1-execution-layer` ¥3,000 の枠内は都度確認せず実行してよい。使ったら `spent_jpy` を更新
- 自分の成果を自分で採点しない。**テストが3回連続で FAIL したら止めて報告する**
- 敵対的検証は**意図的な回避なら1回目から `limits` に宣言して先へ進む**（同一条件は最大2周）

## 6. 検証コマンド

```bash
export GOOGLE_API_KEY=dummy_key_for_ci
PYTHONPATH=./backend python -m pytest \
  backend/tests/test_run_record.py backend/tests/test_cost_guard.py \
  backend/tests/test_model_policy.py backend/tests/test_verify_account.py \
  backend/tests/test_revenue_artifact_gate.py \
  backend/tests/test_video_composer_subtitles.py \
  backend/tests/test_soul_feedback_model_policy.py -q --no-cov

PYTHONPATH=./backend python -m backend.verify_account        # 段の実在（--probe で無料枠も）
PYTHONPATH=./backend python -m backend.model_policy --audit  # 入替トリガー（要 exit 0）
PYTHONPATH=./backend python -m backend.cost_guard --status   # 残高と台帳
PYTHONPATH=./backend python -m backend.revenue.artifact_gate --gate
```

**フルスイートはローカルで OOM する実績がある**（約13.6GB）。CI を正典にする。

## 7. R1 のあと（先走らない）

- **Playground を「設計の場」として使う**案をユーザーに提示済み。
  `soul_feedback` のプロンプトは**一度も人の目で検証されていない**。
  段の優劣も未実測。Playground なら ¥0 で比較できる。**R2 の前に挟むのが良い**
- Gemini 以外の Google API（画像・動画のインサート）構想は**別規約・別無料枠**。
  多くは請求先必須で、今の2キー構成の外に出る。**R1 の後に一次情報を当たる**
