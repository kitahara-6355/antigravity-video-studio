# 引継ぎ 2026-08-16 — ローカル実行への移行

**このセッションで読むのはこの1枚だけでいい。** 他の docs は必要になってから開く。
`CLAUDE.md` と SessionStart フックは自動で入るので重複して読まない。

前セッションは **Web 版 Claude Code**（リモートコンテナ）で走っていた。
素材も ffmpeg も無い環境だったため、**実走だけが未着手**で残っている。
ここからは**ローカルアプリ版**が、素材と実行環境のある手元のフォルダで続きをやる。

---

## 0. 最初にやること — 立ち位置の確認（3分）

**手元のフォルダは、Antigravity 時代の作業ツリーの可能性がある。**
このリポジトリ（公開版）は 2026-07-27 に旧 private リポジトリの作業ツリーから
**履歴を切り離して**作り直したもので、**別物になっている**かもしれない。

```bash
git remote -v          # kitahara-6355/antigravity-video-studio か？
git log --oneline -3   # 88a95aa が見えるか？
git status --short
```

| 結果 | どうするか |
|---|---|
| `88a95aa` が見える | そのまま続行。ブランチは `claude/work-handover-4rgt1u` |
| リモートは同じだが古い | `git fetch origin claude/work-handover-4rgt1u && git checkout claude/work-handover-4rgt1u` |
| **リモートが違う／git 管理外** | **コミットしない。** そのフォルダは「素材と過去記録の置き場」として読むだけにして、コード変更は別途 clone した作業ツリーで行う |

### このフォルダに絶対に入れてはいけないもの

旧ツリーには**履歴に API キーが残っていた**経緯がある。以下は `git add -f` でも入れない。

- `backend/.env`、`*.key`、`vault-assets/raw/**` — **読み取りもしない**
- `Human01_Official Artifact/` — **1ファイルも存在させない**（再生成もしない）
- `antigravity_phase18_stable_v1/`、`antigravity_phase19_experimental_v1/`、`archives/`

---

## 1. いますぐやること

### (1) 過去の完走記録を発掘する ← **最優先。ここに答えがある**

**Antigravity 時代に、ffmpeg と Whisper を入れて個人アカウントの API キーで
自動編集の完走テストを何度も通した記録が、手元の `script` フォルダ配下に
残っているはず**（ユーザー証言・2026-08-16）。

これは推測ではなく**実際に動いた証拠**なので、いまから作るより先に読む。

```bash
ls script* 2>/dev/null
find . -maxdepth 3 -iname "*script*" -type d 2>/dev/null | head
find . -name "*.mp4" 2>/dev/null | head          # 素材があるか
find . -iname "*run*.json" -o -iname "*result*.json" 2>/dev/null | head -20
```

見たいのは4つ:

1. **どのコマンドで完走したか**（エントリポイントは何だったか）
2. **どのモデルを使ったか**（当時は `gemini-2.5-*` 系のはず。**2026-10-16 に終了する**）
3. **1本あたりの所要時間と、落ちた工程**
4. **ffmpeg / Whisper のバージョンと入れ方**

発掘結果は `docs/ANTIGRAVITY_RUN_RECORDS_20260816.md` に要約して残す。
**過去の実績を、いまの `output/runs/<run_id>/run.json` の形に写せるなら写す。**

### (2) 環境の確認

```bash
ffmpeg -version | head -1
ffprobe -version | head -1     # ★ 成果物ゲートに必須
python -c "import whisper; print('whisper OK')"
```

**`ffprobe` があるかどうかが決定的。** 前セッションのコンテナには無く、
そのせいで R1-C1 が判定できなかった。手元にあれば**そこが解決する**。

### (3) API キーの点検

ユーザーが `backend/.env` に `GOOGLE_API_KEY` を置いたら:

```bash
python -m backend.verify_account            # 無料の点検のみ
python -m backend.verify_account --probe    # 最小の生成を1回（無料枠の有無が確定）
```

| 出力 | 意味 | 次 |
|---|---|---|
| `✅ 呼び出しが通りました` | 無料枠が実在 | **実装変更なしで実走へ** |
| `🚫 billing_required` | 無料枠の対象外 | **2キー構成**の実装（下記 §4） |
| `🚫 quota_exhausted` | 枠はある・使い切り | 時間を置いて再実行 |
| `🚫 ai_studio_blocked` | 管理コンソールで OFF | ユーザーに戻す |

段の4モデルの実在（**R1-C7**）も同時に判定する。実在しない ID が出たら
`backend/model_config.json` を直すのは Claude Code の仕事。

### (4) 動画を1本通す ← **R1 の本丸**

```bash
python -m backend.revenue.artifact_gate --semantics   # 何を見るゲートか
python -m backend.revenue.artifact_gate --gate        # いまは正しく FAIL する
```

**各工程を `RunRecorder.stage()` で包んでから走らせる。** 包まないと記録が残らず、
成果物ゲートが `model_not_recorded` で FAIL する（それが仕様）。

```python
from backend.revenue.run_record import RunRecorder

rec = RunRecorder(inputs={"source": "..."})
with rec.stage("transcribe", model="local:whisper", stage_input={...}):
    ...
with rec.stage("telop", task="telop_suggestion", stage_input={...}):
    ...
    rec.artifact("output/final/demo.mp4")
rec.finish()
```

- ローカル処理は `model="local:ffmpeg"` の形で**モデルを使わないことも宣言する**
- `stage_input` を渡さないと、失敗時に**再開できない**とゲートが言う（fail-closed）
- 実測モデルは `cost_guard` 台帳から自動で拾われ、宣言と食い違えば `model_mismatch` に残る

**まず短い素材（10〜30秒）で通す。** 長尺は原価と時間が読めてから。

---

## 2. いまどこにいるか

**現フェーズ R1「実行層（L2）を開けて、動画を1本完走させる」**（正典 = `backend/branding/vision_backlog.json`）

| 条件 | 状態 |
|---|---|
| R1-C1 再生可能な mp4 が1本（ffprobe 判定） | ⬜ **実走待ち** |
| R1-C2 1本あたりの実コストと所要時間 | ⬜ 実走待ち |
| R1-C3 上限に達したら止まる | ✅ 2026-08-15（18 passed） |
| R1-C4 失敗した工程から再開できる | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C5 実走で使ったモデル名が残る | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C6 工程ごとのモデルが記録に残る | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C7 段のモデルが一次情報と突き合わせ済み | ⬜ **キー投入で即確定** |

**残っているのは実走だけ。** 道具は全部揃っている。

`met` は自分で立てない（憲法第4条）。**`/gate-report` はユーザーが打つ。こちらから起動しない。**

---

## 3. 前セッションで作ったもの（全部 push 済み）

ブランチ `claude/work-handover-4rgt1u` / PR #79（draft・CI 3ジョブ緑）

| ファイル | 役割 |
|---|---|
| `backend/cost_guard.py` | **キルスイッチと実費台帳。** `model_governance` の proxy 3箇所が絞り口。予算が無い実キーは例外で停止。未計測・単価不明を無料扱いにしない。思考トークンも計上 |
| `backend/revenue/run_record.py` | **実行記録の書き手。** 工程ごとに原子的に書き出す。宣言モデルと**実測モデル**の両方を残す。`--list` / `--resume <run_id>` |
| `backend/revenue/artifact_gate.py` | **成果物ゲート。** mp4 を ffprobe で実体判定・再開可能性・実費・使用モデル。**ffprobe が無ければ FAIL**（fail-closed） |
| `backend/model_policy.py` | **段の見える化と昇格。** `--show` / `--why` / `--up` / `--audit` / `--triggers` |
| `backend/verify_account.py` | **アカウントの点検。** キー・AI Studio・段の実在・無料枠の有無を実測 |
| `backend/config/gemini_pricing.json` | 単価表・課金体系・無料枠・**spend_cap**（出典と取得日つき） |

直近4コミット:

```
88a95aa fix(cli): backend/.env を CLI が読んでいなかった
7970aa5 feat(r1): アカウントの点検を実測コマンドにする
b1dfc83 fix(billing): 本命の歯止めを Prepay から Spend Cap に訂正する
ea42789 feat(r1): 実行記録の書き手を用意し、モデル点検 CLI の起動不能を直す
```

---

## 4. 未決事項（ユーザー判断待ち・こちらから催促しない）

### (a) キー構成 — **`--probe` の結果で決まる**

P3（採用済み）は「上3段は無料枠の Flash、pro だけ課金」だが、
**課金を有効にすると同じプロジェクトの無料枠が消える**（二次情報）。
1プロジェクト1キーでは P3 が成立しない可能性がある。

- 無料枠が使えた → **1キーのまま。実装変更なし**
- 使えなかった → **2キー構成**（無料 Flash 用 / 課金 Pro 用）。
  `gemini_client_factory` は `GOOGLE_API_KEY` を1本しか読まないので、
  **段（tier）ごとにキーを選ぶ実装**が要る。`model_policy` に寄せれば工程側は無変更

### (b) Drive の扱い — **直結しない方針でユーザーと合意（2026-08-16）**

Google ドライブ デスクトップで共有ドライブをマウントし、パイプラインは
**ローカルパスとして読む**。OAuth もサービスアカウントも要らない。
`scripts/google_oauth_login.py` と `backend/services/workspace_sync.py` は当面使わない。

⚠ ただし `workspace_sync.py` が API 経由を選んだ理由が記録されている:
**ファイル名と中身が食い違った実例がある**（`20251010_..._インタビュー.mp4` の中身が別収録）。
マウントするとファイル ID を失うので、**内容ハッシュで同一性を見る**代替が要る。

### (c) GCP/GWS 上での実行 — **将来課題（ユーザー判断・2026-08-16）**

無料枠は「どこで動かすか」と無関係。クラウドに出すと**いま無料の ffmpeg/Whisper が有料になる**。
本数が増えて手元が律速になってから考える。

### (d) Spend Cap

AI Studio の Spend タブ →「Monthly spend cap」がプロジェクト単位の月額上限。
設定できたら `.claude/budget.json` の `spend_cap_usd` / `spend_cap_project` を埋める。
未設定だと `python -m backend.cost_guard --status` が警告する。

---

## 5. 効いている制約

| 期日 | 何が起きるか |
|---|---|
| **2026-10-16** | **Gemini 2.5 系が提供終了。** リポジトリは `gemini-2.5-flash` を475箇所で参照。**過去の完走記録も 2.5 系のはず** |
| **2027-02-01** | 新規申請者の収益化基準が2倍。現行基準なら残り約5.5ヶ月 |

**外部情報はすべて二次情報**（`confidence: unverified_secondary`）。
一次情報（`ai.google.dev` / `docs.cloud.google.com` / `blog.google`）は
**前セッションの実行環境ではプロキシで遮断されていた。ローカルなら読めるはずなので、突き合わせる。**

---

## 6. 進め方の約束（前セッションから継続）

- **進め方の判断は確認不要。事後報告でよい**（憲法第1条・フェーズ内は単独で決めて実行）
- **`/gate-report` はユーザーが打つ。こちらから起動しない**
- **1円でも請求されるものは事前承認**（憲法第3条）。`R1-execution-layer` = ¥3,000 が承認済みで、
  **この枠内は都度確認せず実行してよい**。使ったら `.claude/budget.json` の `spent_jpy` を更新する
- 自分の成果を自分で採点しない（憲法第4条）。`met` を立てるのは独立検証の後
- テストが3回連続で FAIL したら、続けずに報告して止まる
- 敵対的検証は**意図的な回避なら1回目から `limits` に宣言して先へ進む**（同一条件は最大2周）

## 7. 検証コマンド

```bash
pytest backend/tests/test_run_record.py backend/tests/test_cost_guard.py \
       backend/tests/test_model_policy.py backend/tests/test_verify_account.py \
       backend/tests/test_revenue_artifact_gate.py     # 137 passed が現状

python -m backend.cost_guard --status              # 残高と警告
python -m backend.model_policy --show              # 段の現在地
python -m backend.model_policy --audit             # 実在確認（キー必須・いまは exit 1）
python -m backend.verify_account --probe           # 無料枠の有無
python -m backend.revenue.run_record --list        # 実行記録
python -m backend.revenue.artifact_gate --gate     # 成果物ゲート
```

**フルスイートはローカルで OOM する実績がある**（約13.6GB）。CI を正典にする
（`scripts/run_test_batches.py --batches 8`）。手元で回すならバッチ分割。
