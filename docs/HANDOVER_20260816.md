# 引継ぎ 2026-08-16 — ローカル実行への移行

**このセッションで読むのはこの1枚だけでいい。** 他の docs は必要になってから開く。
`CLAUDE.md` と SessionStart フックは自動で入るので重複して読まない。

前セッションは **Web 版 Claude Code**（リモートコンテナ）で走っていた。
素材も ffmpeg も無い環境だったため、**実走だけが未着手**で残っている。
ここからは**ローカルアプリ版**が、素材と実行環境のある手元のフォルダで続きをやる。

---

## 0. フォルダの立ち位置（2026-08-16 に実測で判明）

**`script/` は親フォルダで、中に複数のツリーが同居している。**

| 場所 | 何か | 扱い |
|---|---|---|
| `script/antigravity-video-studio/` | **現役の公開リポジトリ。コードを書くのはここだけ** | この文書があるツリー |
| `script/video-automation/` | **Antigravity 時代の旧ツリー。読むだけ** | 素材・過去記録・旧 `.env` がある |
| `script/.../My RSS-feed/...` | 本番 RAW 素材の正本 | 読むだけ |

**この文書はブランチ `claude/work-handover-4rgt1u` にしか無い。** 手元が古いと
0815 版しか見えない（2026-08-16 に実際そうなった）。

```bash
cd script/antigravity-video-studio
git fetch origin claude/work-handover-4rgt1u
git checkout claude/work-handover-4rgt1u
git log --oneline -1        # 8a264f2 以降が見えれば正しい
```

### 旧ツリーから持ち込んではいけないもの

旧ツリーには**履歴に API キーが残っていた**経緯があり、そのために公開版は
履歴を切り離して作り直された。**`git add -f` でも入れない。**

- `Human01_Official Artifact/`（旧ツリーに **3,100ファイル超**実在する）— **1ファイルも移さない・再生成しない**
- `backend/.env`、`*.key`、`vault-assets/raw/**` — **読み取りもしない**
- `antigravity_phase18_stable_v1/`、`antigravity_phase19_experimental_v1/`、`archives/`

### このフォルダに絶対に入れてはいけないもの

旧ツリーには**履歴に API キーが残っていた**経緯がある。以下は `git add -f` でも入れない。

- `backend/.env`、`*.key`、`vault-assets/raw/**` — **読み取りもしない**
- `Human01_Official Artifact/` — **1ファイルも存在させない**（再生成もしない）
- `antigravity_phase18_stable_v1/`、`antigravity_phase19_experimental_v1/`、`archives/`

---

## 1. 発掘は完了した（2026-08-16）— **これ以上掘らない**

過去の完走記録の発掘は**実施済み**。以下が実測の結論なので、**再走査しない。**

`Human01_Official Artifact/` の 3,100ファイルと brain の 503セッションは
**掘らなくてよい**。brain 全走査は 277 行ヒットして**全部ノイズだった**
（subagent の worktree がリポジトリを1式ずつ複製していただけ）。

### 環境は生きている。作り直す必要はない

| 要素 | 実測 |
|---|---|
| **ffmpeg / ffprobe** | **8.0.1（PATH 上にある）** ← 前セッションのコンテナに無かったもの。**R1-C1 が判定できる** |
| faster-whisper モデル | 5種キャッシュ済み（tiny / base / small / medium / large-v3） |
| **本番 RAW 1.1GB 4本** | 健在。`My RSS-feed/Youtube/スタジオ収録/AI Studio アップロード用動画/` に シーン01 600.5MB / 02 250.9MB / 03 139.0MB / 04 109.5MB |
| 旧ツリーの個人 API キー | `video-automation/backend/.env` に実在（54 bytes・2026-04-01）**中身は読まない** |
| Whisper 実行痕跡 | `vault-assets/raw_videos/本番RAW01 対談_久木田/_whisper_segments.jsonl` |

**消えているもの**: 完成動画は2本とも現存しない。`vault-outputs/final/` にあるのは
**全部 2,097,152 バイト（2MB ちょうど）のダミー**。パイプラインの入力フォルダには
シーン02しか無い（正本は `My RSS-feed` 側にあるので置き直せば再現可能）。

### 過去の完走実績（2026-06-27〜28・セッション `85253b88`）

- 本番 RAW 4本 1.1GB の E2E が**完遂**。`final_20260628_022409.mp4` 125.6MB / **17分55秒**、
  ロゴ重畳・ラウドネス正規化 −16 LUFS 済み
- **ただし成功率は 8回試行して1回（12.5%）。** 7回は環境要因で落ちている
- 稼働 約16時間・総ステップ 3,355+
- **見つけて直したバグ**: `logo_overlay.py` の ffmpeg タイムアウト **60秒 → 1800秒**。
  本番尺で初めて顕在化する。**再走で同じ所に当たる可能性がある**
- 再現用ランナー: `scratch/run_e2e_real_raw.py`（Playwright で UI を叩き、
  `/api/pipeline/status` を10秒間隔でポーリング。Whisper 待ち最大45分・レンダ待ち最大20分）

### ⚠ 記録の信用度に留保がある

同じ `85253b88` セッションが並行して「v6.0 の 1,045項目 全PASS」を**架空データだったと
自ら認定している**（実 PASS 率 7.18%）。実際 `e2e_user_manual.md` は「🟢 完全PASS」と
書きながら、貼っているスクショが `e2e_step5_timeout.png`（タイムアウト分岐）。

**採用するのは `session_analysis_and_best_practices.md` の「1回/8回・12.5%」のほう。**
「過去に通った」は「**再現性がある**」を意味しない。

### だから R1 は過去実績では閉じない

R1-C1 は「**再生可能な mp4 が1本存在する**」。完成動画は**現存しない**ので、
**もう一度通すしかない。** 素材と実行環境は揃っているので、やることは実走だけ。

### (3) API キーの点検

> [!IMPORTANT]
> **2026-08-17 訂正。** ここには当初「個人アカウントなら**無料枠が使える**ので
> これで足りる可能性が高い」と書いていた。**これは誤り。**
> ユーザーが AI Studio の画面を実測で示した結果、旧ツリーのキーが載っている
> プロジェクトは**課金キー**だった。下の表が実測。

| キー | プロジェクト | 作成 | 請求階層 |
|---|---|---|---|
| `...KqrA` | `video-automation-489607` | 2026-03-08 | **請求先アカウント 有効1・Tier 1・後払い**（＝課金） |
| `...U6uw` | `gen-lang-client-0733068258`（Jigyokei） | 2026-07-31 | **無料枠** |

**旧ツリーのキーは `video-automation/backend/.env`（2026-04-01）にあり、
作成日 2026-03-08 の `...KqrA` と時系列が整合する**（中身は読まないので確定ではない）。
つまり**旧ツリーのキーを置くと課金経路に載る。**

**ただし実績は ¥0。** 請求先アカウント `01B675-738297-BB2BD0` の利用履歴は
**6月 ¥0 / 7月 ¥0 / 8月1〜16日 ¥0**、最後の請求は 4月1日の **¥1**。
**2026-06-27〜28 の 1.1GB 完走を含む6月が ¥0** なので、課金キーでありながら
Flash の無料枠に収まっていたことになる。**これは R1-C2 の有力な傍証** —
過去の完走は実質 ¥0 で通っている。

- 階層の上限: **¥39,129**（Tier 1）／支払い方法は**個人カード ····1528**
- **⚠ 前払いへの強制移行の警告が出ている** — 「このアカウントでは前払いによる
  お支払いを使用する必要があります。サービスが中断しないように、今すぐ切り替えと
  クレジットの購入を」。**放置すると中断する。**
  切り替えとクレジット購入は金銭の操作なので**ユーザー作業**（§4e）

#### 使うキーは `...KqrA` で確定（2026-08-17・規約の一次情報で決着）

**一度は「無料枠だけで済ませたいなら `...U6uw`」と書いたが、それは誤り。**
ユーザーが AI Studio の「アカウントステータス → 有料サービスの利用規約」から
原文を出してくれて、判定軸が分かった。

> **Cloud 請求先アカウントを有効化すると、Gemini API および Google AI Studio の
> すべての使用は、「Google による使用者のデータの利用方法」に関しては
> 「有料サービス」となります。これは、無料で提供される本サービス
> （Google AI Studio、Gemini API の無料枠など）を使用する場合でも同様です。**
> — Gemini API 追加利用規約「有料サービス」

**学習に使われるかどうかは「無料枠かどうか」では決まらない。「請求先アカウントが
有効かどうか」で決まる。** 有料サービス扱いなら Google はプロンプトと回答を
プロダクトの改善に使わない。

| キー | 請求先 | データの扱い |
|---|---|---|
| **`...KqrA`**（video-automation） | **有効** | **有料サービス扱い。無料枠でも学習に使われない** |
| `...U6uw`（Jigyokei） | 未設定 | 無料サービス扱い。学習に使われる |
| 法人 `antigravity-pipeline` | 未設定 | 同上（かつ現在アクセス制限中） |

##### ⚠ ここを「素材が使用不可」と読まないこと（2026-08-17 訂正）

無料サービス側には「プライベート情報、機密情報、または個人情報を送信しないで
ください」とあるが、**これは禁止条項ではない。**

- 本物の禁止は**「使用制限」**の節にあり、「使用**してはなりません**」「**禁止されています**」と書く
- この一文は**「データの利用方法」**の節の説明の流れに置かれた「送信**しないでください**」
  （原文 "Please don't submit…"。登録メールでも "**As a reminder**" として登場する）

**リスクを説明して判断を利用者側に置いた注意喚起であって、対談素材が
使えなくなるわけではない。** 一度「規約が明示的に禁じている」と書いたのは
読み込みすぎだった。

**実測（2026-08-17）— 何が送られるか。** パイプライン10工程のうち Gemini を
呼ぶのは **`soul_feedback_engine` の1つだけ**（`_generate_suggestions_via_llm`）。
送るのは**文字起こしテキスト全文を1プロンプトに埋めた1回の呼び出しのみ**。
**映像・顔・静止画・音声は一切送信されない**（Whisper もサムネイル抽出もローカル ffmpeg）。

判断材料は両側にある:

| 軽いと言える側 | 慎重に見るべき側 |
|---|---|
| 素材は YouTube 公開前提。公開後は誰でも書き起こせる | **編集前の素材は未公開。最終的にカットする発言も全部送られる** |
| 内容は職業上の発言で、要配慮個人情報ではない | 「YouTube に出る同意」と「機械学習の訓練に使われる同意」は別物 |
| 人間レビュー前にアカウント・キー・プロジェクトから切り離される | Whisper の誤認識で意図しない語が混ざる |

**どちらを取るかはユーザーの判断。**

##### ✅ 決着（2026-08-17 ユーザー決定・**全承認期間**）

**この議論は終わっている。蒸し返さないこと。**

- **1人ユーザー体制では無料枠を最大限活用する。対談相手には了承を得たうえで API を使う**
- **2人以上に展開する場合は、追加ユーザー分のみ有料体制**とする。開発者兼メインユーザーで
  ある第一ユーザーは引き続き無料枠を積極的に使う
- 第一ユーザーの利用は無料枠最大化による実験的試みであり、開発者枠に収まっているという
  ユーザーの判断
- **ユーザーが引いた線は『BAN・パージされない限り』の1本だけ。** そこだけ守る

なお EEA・スイス・英国拠点なら無料サービスにも有料の条項が適用されるが、
**日本は対象外**なのでこの救済は効かない（参考情報）。

##### 使うキーは請求先が**紐づいていない**プロジェクトのもの

**一次情報（2026-08-17）:**

> Free tier requires only an active project, while **Tier 1 requires linked billing**.
> **Rate limits are applied per project, not per API key.**
> — https://ai.google.dev/gemini-api/docs/rate-limits

**Free tier の条件は「請求先が紐づいていないこと」。** 請求先を有効にした時点で
そのプロジェクトは Tier 1（課金）に移り、無料枠ではなくなる。

> [!IMPORTANT]
> **無料枠最大化は「通常運転」の話であって、`pro` 昇格（課金）を否定しない。**
> **P3 のモデル選定基準は 2026-08-16 の承認から一切変わっていない**（2026-08-17 ユーザー確認）。
> 北極星は YouTube 収益化なので、**品質は考慮事項として生き続ける。**
> 成果物に不満があれば段を上げて焼き直す — ¥3,000 はそのために温存してある。
> 「無料だから品質を諦める」は方針の読み違い。

##### これで §4(a) の「2キー構成が要るか」が確定した

- Free tier の条件 = **請求先が紐づいていないこと**
- `pro` の課金 = **請求先が必要**

**同一プロジェクトでは両立しない。したがって2キー構成は確定で必要**（「かもしれない」ではない）。

**構成とキー管理の正典は `CLAUDE.md` の「API プロジェクトとキーの管理」**
（2026-08-17 ユーザー決定）。ここでは要点だけ:

| 用途 | プロジェクト | アカウント | 請求先 | env |
|---|---|---|---|---|
| **通常運転**（batch/standard/premium） | `avs-prod-free` | **法人** | なし | `GOOGLE_API_KEY` |
| **昇格専用**（pro） | `avs-prod-paid` | **法人** | あり | `GOOGLE_API_KEY_PRO` |
| 開発・実験 | `avs-dev-free` | 個人 | なし | dev 側の `.env` |

- **本番は法人に置く**（事業主体が美麗書院。経費・資産帰属・継承が素直）
- **1プロジェクト1キー。** キーを増やしても枠は増えない
- **R1 の間は個人の `video-automation`（`...KqrA`）を暫定で使う。** 動かすのが最優先。
  法人の請求先が通ったら本番を移して個人側を閉じる

> [!WARNING]
> **一度「通常運転は `...U6uw`」と書いたが撤回した。**
> `...U6uw` は **jigyokei-app のプロジェクト**のキーで、AVS に流用すると
> 別プロダクトのプロジェクトを使い回すことになる。
> **名前と用途が食い違うのは、この1年のキー管理の混乱そのものの型。触らない。**

実装（台帳・突き合わせコマンド・段ごとのキー選択層）は **R1 の後**
（2026-08-17 ユーザー決定）。`gemini_client_factory.py` の `_get_api_key()` が
**唯一の読み口**なのでそこに寄せる。工程側は無変更で済む。
なお `backend/agents/council_graph.py:219` が `GEMINI_API_KEY`（旧名）を読んでいて
**変数名が2種類混在している。これも混乱の一因なので一本化する。**

**副次的な利点: 通常運転側は無料枠を超えたら課金ではなく 429 で止まる。**
意図しない請求が構造的に起きない。課金は `pro` を指示したときだけに閉じる。

**レート制限はプロジェクト単位（キー単位ではない）。** 2人以上に展開して
ユーザーを分離するなら、キーではなくプロジェクトを分ける。

##### ⚠ 「6月が ¥0 だから無料枠に収まっていた」は誤った推論だった（2026-08-17 撤回）

Tier の仕組みが分かった以上、この推論は成り立たない。`...KqrA` は Tier 1 なので、
呼んでいれば課金されていたはず。

**より単純な説明がある。** 2026-06-28 のセッション記録に
「`_generate_suggestions_via_llm` は**実装済み・テスト未実施**」
「ステップ4テスト未実施が最優先の残タスク」と明記されている。つまり
**あの 1.1GB 完走は Gemini をほとんど、あるいは一度も呼んでいなかった**可能性が高い。

**したがって ¥0 の実績は「本番1本が無料で回る証拠」ではない。
原価はまだ誰も測っていない、が正しい現在地。R1-C2 はここから測る。**

##### BAN されないために実装で守ること

一次情報を当たったが、**quota evasion や複数プロジェクトでの回避に関する
明示的な記述は見つからなかった**（無いものを有ると書かない）。
書いてあるのは 429 への対処だけ:

> Wait and retry after a short period / Reduce the rate of expensive requests

**守るのは1点。429 が返ったときにリトライを連打しない**（指数バックオフ）。
連打は自動化された乱用と区別がつかない。ユーザーの方針
（1人体制は無料枠・追加ユーザーは有料）は複数アカウントで無料枠を積み増す形では
ないので、回避のパターンには当たらない。

詳細は `backend/config/gemini_pricing.json` の `data_use`（**このリポジトリで
唯一 `verified_primary` の項**）。

法人キーを新規発行する話（§4a）は**R1 の後でよい**。R1 の目的は原価の実測で、
無料枠でも `cost_guard` は「課金されたと仮定した額」を台帳に積むので、
**¥0 で有料時の見積もりが取れる。**

ユーザーが `antigravity-video-studio/backend/.env` に `GOOGLE_API_KEY` を置いたら:

```bash
python -m backend.verify_account            # 無料の点検のみ
python -m backend.verify_account --probe    # 最小の生成を1回（無料枠の有無が確定）
```

| 出力 | 意味 | 次 |
|---|---|---|
| `✅ 呼び出しが通りました` | 無料枠が実在 | **実装変更なしで実走へ** |
| `🚫 billing_required` | 無料枠の対象外 | **2キー構成**の実装（下記 §4） |
| `🚫 quota_exhausted` | 枠はある・使い切り | 時間を置いて再実行 |
| `🚫 ai_studio_blocked` | 管理コンソールで OFF | ユーザーに戻す |

段の4モデルの実在（**R1-C7**）も同時に判定する。実在しない ID が出たら
`backend/model_config.json` を直すのは Claude Code の仕事。

### (4) 動画を1本通す ← **R1 の本丸**

```bash
python -m backend.revenue.artifact_gate --semantics   # 何を見るゲートか
python -m backend.revenue.artifact_gate --gate        # いまは正しく FAIL する
```

**2回に分ける。** 過去の成功率が 12.5% なので、**1回目からゲートの体裁を
整えようとすると、パイプラインの不具合と記録コードの不具合を同時に相手にする**ことになる。

| | やること | 取れるもの |
|---|---|---|
| **1回目** | 既知の通った経路（`scratch/run_e2e_real_raw.py` の系統）で**素直に通す** | **mp4（R1-C1）**・所要時間・落ちる箇所 |
| **2回目** | `RunRecorder` を被せて走らせる | 実行記録（**R1-C4/C5/C6**）・原価（**C2**） |

無料枠なら2回走らせても ¥0。**まず短い素材（10〜30秒）で1回目**を通し、
本番 RAW（17分55秒）は経路が確定してから。

**`logo_overlay.py` の ffmpeg タイムアウトを最初に確認する** — 過去に本番尺で
顕在化して 60秒 → 1800秒 に直した箇所。公開版に同じ修正が入っているとは限らない。

#### ✅ 1回目は実施済み（2026-08-17・費用 ¥0・キー不要）

**UI 経由（`run_e2e_real_raw.py`）ではなく、`pipeline_coordinator` の CLI で通した。**
そちらのほうが速く、落ちた箇所が直に出る。Playwright もサーバ2本も要らない。

```bash
# シーン03 から30秒を切り出して通す。Gemini 呼び出しゼロ・¥0
ffmpeg -y -ss 60 -t 30 -i "<My RSS-feed>/シーン03_後編01.mp4" -c copy output/testclips/r1_clip30s.mp4
PYTHONPATH=. python -m backend.video_pipeline.pipeline_coordinator output/testclips/r1_clip30s.mp4
```

- **10/10 ステージ完走・33.7秒**（30秒素材）。うち Whisper が大半、quality_gate 4.6秒
- `soul_feedback` は Gemini が要るが `pipeline_coordinator.py:444` の except に落ちて
  スキップされる。**だからキー無しで通る**
- `logo_overlay.py:142` は `timeout=1800.0` が**公開版にも入っていた**。ここは再発しない

**⚠ ただし1回目は「10/10 完走・品質ゲート 89.2点 PASS」で終わりながら、
成果物が入力と SHA256 まで一致していた**（字幕が1文字も焼かれていない）。
原因と修正は `e78f686`:

- `-vf subtitles='<path>'` の中で `\` がエスケープ文字として食われ、
  Windows のパス区切りが消えて ffmpeg が開けない。**CI は Linux なので原理的に再現しない**
- その失敗を `add_subtitles()` の `return ""` と `compose()` の `if subtitled:` が
  二重に握り潰し、`success=True` を返していた

**教訓（次に効く）**: **完走したら必ず成果物のハッシュを入力と比べる。**
ステージの success と成果物の中身は別物で、このパイプラインは
「何もしなかった」を成功として返す箇所がまだある可能性がある。

**残っている既知の劣化**（R1 の合否には効かないが成果物の質に効く）:

- `ComposeResult.duration` が **5.0 直書き**。29.53秒の動画に 5.0 を返す
- **MeCab 辞書が無く**、日本語解析が毎回 正規表現にフォールバック（字幕の行分割が劣化）
- Whisper が `base` 固定で、日本語の固有名詞が崩れる。
  旧ツリーにあった**固有名詞辞書139件はこの経路に入っていない**

2回目に被せる書き方:

**各工程を `RunRecorder.stage()` で包んでから走らせる。** 包まないと記録が残らず、
成果物ゲートが `model_not_recorded` で FAIL する（それが仕様）。

```python
from backend.revenue.run_record import RunRecorder

rec = RunRecorder(inputs={"source": "..."})
with rec.stage("transcribe", model="local:whisper", stage_input={...}):
    ...
with rec.stage("telop", task="telop_suggestion", stage_input={...}):
    ...
    rec.artifact("output/final/demo.mp4")
rec.finish()
```

- ローカル処理は `model="local:ffmpeg"` の形で**モデルを使わないことも宣言する**
- `stage_input` を渡さないと、失敗時に**再開できない**とゲートが言う（fail-closed）
- 実測モデルは `cost_guard` 台帳から自動で拾われ、宣言と食い違えば `model_mismatch` に残る

**まず短い素材（10〜30秒）で通す。** 長尺は原価と時間が読めてから。

---

## 2. いまどこにいるか

**現フェーズ R1「実行層（L2）を開けて、動画を1本完走させる」**（正典 = `backend/branding/vision_backlog.json`）

| 条件 | 状態 |
|---|---|
| R1-C1 再生可能な mp4 が1本（ffprobe 判定） | ⬜ **実走待ち** |
| R1-C2 1本あたりの実コストと所要時間 | ⬜ 実走待ち |
| R1-C3 上限に達したら止まる | ✅ 2026-08-15（18 passed） |
| R1-C4 失敗した工程から再開できる | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C5 実走で使ったモデル名が残る | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C6 工程ごとのモデルが記録に残る | ⬜ 実走待ち（**仕組みは完成**） |
| R1-C7 段のモデルが一次情報と突き合わせ済み | ⬜ **キー投入で即確定** |

**残っているのは実走だけ。** 道具は全部揃っている。

`met` は自分で立てない（憲法第4条）。**`/gate-report` はユーザーが打つ。こちらから起動しない。**

---

## 3. 前セッションで作ったもの（全部 push 済み）

ブランチ `claude/work-handover-4rgt1u` / PR #79（draft・CI 3ジョブ緑）

| ファイル | 役割 |
|---|---|
| `backend/cost_guard.py` | **キルスイッチと実費台帳。** `model_governance` の proxy 3箇所が絞り口。予算が無い実キーは例外で停止。未計測・単価不明を無料扱いにしない。思考トークンも計上 |
| `backend/revenue/run_record.py` | **実行記録の書き手。** 工程ごとに原子的に書き出す。宣言モデルと**実測モデル**の両方を残す。`--list` / `--resume <run_id>` |
| `backend/revenue/artifact_gate.py` | **成果物ゲート。** mp4 を ffprobe で実体判定・再開可能性・実費・使用モデル。**ffprobe が無ければ FAIL**（fail-closed） |
| `backend/model_policy.py` | **段の見える化と昇格。** `--show` / `--why` / `--up` / `--audit` / `--triggers` |
| `backend/verify_account.py` | **アカウントの点検。** キー・AI Studio・段の実在・無料枠の有無を実測 |
| `backend/config/gemini_pricing.json` | 単価表・課金体系・無料枠・**spend_cap**（出典と取得日つき） |

直近4コミット:

```
88a95aa fix(cli): backend/.env を CLI が読んでいなかった
7970aa5 feat(r1): アカウントの点検を実測コマンドにする
b1dfc83 fix(billing): 本命の歯止めを Prepay から Spend Cap に訂正する
ea42789 feat(r1): 実行記録の書き手を用意し、モデル点検 CLI の起動不能を直す
```

---

## 4. 未決事項（ユーザー判断待ち・こちらから催促しない）

### (a) キー構成 — **`--probe` の結果で決まる**

P3（採用済み）は「上3段は無料枠の Flash、pro だけ課金」だが、
**課金を有効にすると同じプロジェクトの無料枠が消える**（二次情報）。
1プロジェクト1キーでは P3 が成立しない可能性がある。

- 無料枠が使えた → **1キーのまま。実装変更なし**
- 使えなかった → **2キー構成**（無料 Flash 用 / 課金 Pro 用）。
  `gemini_client_factory` は `GOOGLE_API_KEY` を1本しか読まないので、
  **段（tier）ごとにキーを選ぶ実装**が要る。`model_policy` に寄せれば工程側は無変更

**2026-08-17 の実測で材料が増えた**（§1(3) の表）。手元には3つの経路がある:

| 経路 | 状態 | R1 への向き |
|---|---|---|
| 個人 `...KqrA`（video-automation） | Tier 1・後払い。**実績は6〜8月すべて ¥0** | **本命。** 過去の完走が通った経路そのもの |
| 個人 `...U6uw`（Jigyokei） | 表示上**無料枠** | 無料に振り切るならこちら。別プロジェクトなので spend cap も別 |
| 法人 `antigravity-pipeline` | **請求先未設定で API アクセス制限中** | いま使えない。請求先を通すまで待ち |

「課金を有効にすると無料枠が消える」という二次情報は、**個人 `...KqrA` の実績 ¥0 と
整合しない。** 課金が有効でも Flash の無料枠は残っていた可能性がある。
**`--probe` で確定させるまで、どちらの説も採らない。**

### (e) **前払いへの強制移行 — 期限のある宿題（2026-08-17 判明）**

個人の請求先アカウント `01B675-738297-BB2BD0` に警告が出ている:

> このアカウントでは、前払いによるお支払いを使用する必要があります。
> サービスが中断しないように、今すぐ切り替えとクレジットの購入を行ってください。

**放置すると中断する。** 切り替えとクレジット購入は金銭の操作なので
**Claude Code はやらない。ユーザー作業。**

判断材料（CLAUDE.md §課金体系より）:
- **クレジットは1年で失効する**
- **Postpay 移行以外の理由で Prepay を閉じると残高が没収される**
- したがって**買う額は最小限に**。R1 の実測は ¥0 で通る見込みなので、
  急いで多額を積む理由は無い

なお CLAUDE.md は「本命は Spend Cap で、Prepay は上位互換ではない」と結論しているが、
**これは Google 側が Prepay を強制していない前提だった。** 選べるなら Spend Cap、
強制されるなら従うしかない。この訂正を CLAUDE.md にも反映すること。

#### 月額購読（Google AI プラン）は買っても無駄（2026-08-17・一次情報）

法人アカウントで「アップグレードして…」のモーダルが出て、
**「ジェミニAPI（リクエストごと）」と「Google AI（月額購読）」の2択**が示される。
画面は Google 翻訳が掛かっており「継続する」と読めるが、原文は
"Continue with …" で**「この選択で進む」の意味。加入済みという意味ではない。**

> **AI Studio UI only:** Google AI plan benefits for developer usage apply only
> within the Google AI Studio web interface. Direct use of the Gemini API
> (such as using API keys or external applications) is billed and managed separately.
> — https://ai.google.dev/gemini-api/docs/google-ai-plans

**月額購読は AI Studio の画面の中だけに効く。** パイプラインは API キーで外から
叩くので**1円も効かない。法人の「API アクセス制限中」は購読では解けない** —
解くのは Cloud 請求先アカウントの設定だけ。

#### 🚫 法人アカウントでは Gemini API の生成が通らない（2026-08-19 実測）

**法人 `info@kitahara-birei.com` の2プロジェクトが、まったく同じ 403 を返す。**

| キー | プロジェクト | models.list | generate |
|---|---|---|---|
| `…4LVQ` (`avs-prod-free`) | `antigravity-pipeline-504015` | ✅ 50件 | 🚫 403 |
| `…my7A` (`avs-probe-default`) | `gen-lang-client-0204188687` | ✅ 50件 | 🚫 403 |
| `…U6uw` (Jigyokei・個人由来) | `gen-lang-client-0733068258` | — | UI 表示は**無料枠** |

```
403 PERMISSION_DENIED
Your project has been denied access. Please contact support.
```

**独立した2プロジェクトで同一** ⇒ プロジェクト固有ではなく
**アカウント／Workspace 組織の単位**の話。**3つ目を作っても同じ**なので作らない。

**読み取り（models.list）は通り、生成だけ 403。** AI Studio の Playground 自体は
使えている（ユーザーが操作できている）ので、AI Studio 全体が止められているのではない。

**原因は確定していない。** エラー本文が『サポートに連絡せよ』で、
請求先を促す UI の文言（『お支払い情報を設定してください』）と食い違っている。
考えられる筋は3つあるが、**エラーだけでは判別できない**:

1. **Workspace 管理コンソールのポリシー** — ユーザーが管理者なので確認できる。**無料で即効の可能性があるのでここから**
2. **請求先が必要**（UI が言っている道）— 無料枠を諦めることになる
3. アカウントが新しすぎる／検証待ち（2026-08-16 に AI Studio 登録したばかり）

##### R1 を止めないための退避

法人が解決するまで、**個人アカウントに請求先なしの新規プロジェクトを作れば
Free tier が使える見込み**（同じ個人アカウントの Jigyokei が「無料枠」表示）。
**R1 の目的は原価の実測なので、これで通せる。**
本番の所有を法人に移すのは、法人側が解決してからでよい。
**`...U6uw`（Jigyokei）は流用しない** — 別プロダクトの本番キー。

### (b) Drive の扱い — **直結しない方針でユーザーと合意（2026-08-16）**

Google ドライブ デスクトップで共有ドライブをマウントし、パイプラインは
**ローカルパスとして読む**。OAuth もサービスアカウントも要らない。
`scripts/google_oauth_login.py` と `backend/services/workspace_sync.py` は当面使わない。

⚠ ただし `workspace_sync.py` が API 経由を選んだ理由が記録されている:
**ファイル名と中身が食い違った実例がある**（`20251010_..._インタビュー.mp4` の中身が別収録）。
マウントするとファイル ID を失うので、**内容ハッシュで同一性を見る**代替が要る。

### (c) GCP/GWS 上での実行 — **将来課題（ユーザー判断・2026-08-16）**

無料枠は「どこで動かすか」と無関係。クラウドに出すと**いま無料の ffmpeg/Whisper が有料になる**。
本数が増えて手元が律速になってから考える。

### (d) Spend Cap

AI Studio の Spend タブ →「Monthly spend cap」がプロジェクト単位の月額上限。
設定できたら `.claude/budget.json` の `spend_cap_usd` / `spend_cap_project` を埋める。
未設定だと `python -m backend.cost_guard --status` が警告する。

---

## 5. 効いている制約

| 期日 | 何が起きるか |
|---|---|
| **2026-10-16** | **Gemini 2.5 系が提供終了。** リポジトリは `gemini-2.5-flash` を475箇所で参照。**過去の完走記録も 2.5 系のはず** |
| **2027-02-01** | 新規申請者の収益化基準が2倍。現行基準なら残り約5.5ヶ月 |

**外部情報はすべて二次情報**（`confidence: unverified_secondary`）。
一次情報（`ai.google.dev` / `docs.cloud.google.com` / `blog.google`）は
**前セッションの実行環境ではプロキシで遮断されていた。ローカルなら読めるはずなので、突き合わせる。**

---

## 6. 進め方の約束（前セッションから継続）

- **進め方の判断は確認不要。事後報告でよい**（憲法第1条・フェーズ内は単独で決めて実行）
- **`/gate-report` はユーザーが打つ。こちらから起動しない**
- **1円でも請求されるものは事前承認**（憲法第3条）。`R1-execution-layer` = ¥3,000 が承認済みで、
  **この枠内は都度確認せず実行してよい**。使ったら `.claude/budget.json` の `spent_jpy` を更新する
- 自分の成果を自分で採点しない（憲法第4条）。`met` を立てるのは独立検証の後
- テストが3回連続で FAIL したら、続けずに報告して止まる
- 敵対的検証は**意図的な回避なら1回目から `limits` に宣言して先へ進む**（同一条件は最大2周）

## 7. 検証コマンド

```bash
pytest backend/tests/test_run_record.py backend/tests/test_cost_guard.py \
       backend/tests/test_model_policy.py backend/tests/test_verify_account.py \
       backend/tests/test_revenue_artifact_gate.py     # 137 passed が現状

python -m backend.cost_guard --status              # 残高と警告
python -m backend.model_policy --show              # 段の現在地
python -m backend.model_policy --audit             # 実在確認（キー必須・いまは exit 1）
python -m backend.verify_account --probe           # 無料枠の有無
python -m backend.revenue.run_record --list        # 実行記録
python -m backend.revenue.artifact_gate --gate     # 成果物ゲート
```

**フルスイートはローカルで OOM する実績がある**（約13.6GB）。CI を正典にする
（`scripts/run_test_batches.py --batches 8`）。手元で回すならバッチ分割。
