# 中間ゲート報告 — R2-C1〜C3（2026-09-26・クラウドのセッション）

## 1. 判断してほしいこと

> R2 の中間ゲート（C1〜C3）を通してよいか。通すなら、次に **R2-C5（承認画面からのモデルの可視化と昇格）** に進むか、**R3 を先にする**か。

前提として、**本物の置き場での `--gate` exit 0 はローカルでの確認が残っている**（クラウドには `vault-outputs/`・`output/runs/` が無い）。
正典の `met` は、その確認の後に立てる想定で **まだ `false` のまま**にしてある。

## 2. 終了条件の充足（gate-verifier 10周目・HEAD `06570e2`）

| 条件 | 判定 | 証拠 |
|---|:---:|---|
| **R2-C1** 承認していない動画は書き出せない | ✅（ローカルの確認待ち） | 一時の置き場で、本線を AI 工程だけ差し替えて走らせ、プレビューは ffmpeg で実際に作った。CLI・`RenderWorker`（ffmpeg）は本物。<br>・承認前: `--export` rc=1（置き場は空のまま）、`--gate` rc=1（承認待ち）<br>・`--approve --by claude-code` → `--export` rc=0 → `--gate` rc=0<br>・品質95（改善ループ後の値）で production（D-41）<br>・範囲内の反証なし。1〜8周目の抜け道はすべて塞いだ（引継ぎ §2） |
| **R2-C2** 承認の証跡が動画ごとに残る | ✅ | `--trace <run_id>` rc=0<br>・提案: 時刻・プレビュー・品質・モード・使ったモデル<br>・承認: 時刻・承認者・メモ・**人が直した差分（unified diff）**・開示の判断・AI を使った工程<br>・書き出し: 時刻・動画・モード<br>・承認し直した場合、古い承認は `approval_history/` に残る（9周目） |
| **R2-C3** 開示のメタデータが出力に含まれ、欠けたら書き出しが止まる | ✅ | ・サイドカーに `ai_disclosure` の4項目が入る<br>・サイドカーの書き込みに ENOSPC を注入すると「書き出しを止めました（完成品は捨てた）」rc=1、`run.json` は failed、`export.json` は無し、置き場に mp4 は残らない<br>・古い実走のサイドカーを消すと、最新が緑でも置き場の監査が rc=1<br>・承認の開示から項目を抜くと `--export` rc=1<br>・変異2本はどちらも落ちる |

**退行の確認**
- 関連15ファイル: HEAD 380 passed / HEAD^ 374 passed。失敗は両側とも0件（gate-verifier）
- 関連69ファイル: 失敗の集合が HEAD と一致した（どちらも 182件で、この環境に由来する。私の実行）
- `c4_inventory --gate`・`export_sites --gate`・`ui_api_ratchet --ratchet`・正典の整合テストは緑
- `c4_response_gate --gate` は手元では両側とも赤。原因は環境差（`GET /api/models` が 200 か 500 か）で、CI では緑

**CI**
- `be9eb32` の run 36222190944 は3ジョブ緑（台帳ゲートを含む）
- `aefa7e3` の run 36224224196 はテストスイートが success
- `06570e2` の run 36226163975 は §8 に記録する

## 3. スコアの変化

未計測。中間ゲートなので `/vision-audit` は回していない（フェーズ終了時に回す）。

## 4. このフェーズで入れた変更

PR [kitahara-6355/antigravity-video-studio#85](https://github.com/kitahara-6355/antigravity-video-studio/pull/85)（24コミット）。要点は次のとおり。
- 本線を提案で止め、承認と書き出しの門を作った（`88603e3`）。画面からの強制書き出しも承認を通す（`1aa96bb`）
- 書き出す主体（`RenderWorker`）そのものに門を置いた（`038c009`）。書き出し口を列挙する走査ゲートを作った（`c765f33`）
- **置き場の監査**を入れた（`e83b6f4` → `5e121b2` → `1b834c6`）。書き方ではなく結果を見る。置けるのは承認済みの完成品と付属物だけ
- 見ていないものは承認できない — プレビューを必須にし、素材の指紋を取る（`7d33891`）。承認者は `--by` で必ず書く
- 並列の書き出しで名前を取り合わない。同じ実走を二重に書き出さない（`709eb22`・`44236ea`）
- **開示が出力に載らなければ書き出しを止める。** 置き場の監査も開示を問う（`06570e2`）

## 5. 想定と違ったこと

- **C1 は10周かかった。** 反証は次の順に移った。
  1. 書き出しの経路（1〜2周目）
  2. 走査の網羅（3周目）
  3. 監査の穴（4〜5周目）
  4. ファイルを細工する相手（6周目）

  条文に「書き出し」の定義（9-25）と守る範囲（9-26）を足してから収束した。**定義と守る範囲は計画の段で書くべきだった**（`verification_policy` の規則4）
- **C3 は 1〜8周目で met だったのに、9周目で崩れた。** 「書けなくても実行は止めない」という R1.5-C3 のサイドカーの作りが、C3 の「欠けたら止まる」とぶつかっていた。前フェーズの設計判断が次の条件と衝突しうる、という例
- **クラウドでは本物の置き場が見えない。** 最終確認がローカルに残る（§1）

## 6. 次フェーズの提案

- **R2-C5**（承認画面でモデルを見せ、その場で昇格する）で R2 を閉じる。C1〜C3 で承認は CLI だけになっている（limits #5）ので、画面が無いと人が使いにくい
- ただし **2026-10-16 の Gemini 2.5 系の提供終了**が迫っている（475箇所で参照）。R3 に移行が入っているなら、そちらを先にする判断もありうる。**決めるのはユーザー**

## 7. 積み残し（安全側。台帳には未登録）

- **m1**: サイドカーが途中まで書けてから I/O で落ちると、途中までの `.youtube.json` が置き場に残る。監査が赤で捕まえ、mp4 は残らない（10周目）
- **m2**: 最新の実走のサイドカーが壊れた JSON だと、`--gate` が例外で落ちる（rc=1）。書き手はこの状態を作らない（10周目）
- **M1・M2**（9周目）は limits #20・#21 に宣言した。範囲外の1件は #22

## 8. ローカルに戻ったらやること

1. `python -m backend.revenue.approval_gate --gate` を本物の置き場で実行し、exit 0 を確かめる。赤なら、開示のサイドカーの無い承認済みの完成品が無いかを見る（10周目の注記）
2. 緑なら、正典の C1〜C3 の `met` を立てる（証拠として、この報告と gate-verifier 10周目の結果を添える）
