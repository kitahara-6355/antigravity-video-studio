# Antigravity Backend Dependencies
# Phase A 更新 (2026-03-29): LangGraph/LangChain 削除 → google-adk 追加
# Phase D/E 更新 (2026-03-30): moviepy 廃止、vault-environments 互換

# Core Framework
fastapi>=0.115.0
uvicorn>=0.34.0
python-dotenv>=1.0.0
python-multipart>=0.0.20

# Database & State
pydantic>=2.10.0

# AI & LLM (Phase A: ADK統一 - LangGraph/LangChain 全廃)
google-adk>=1.0.0
google-genai>=1.11.0

# Subtitle Engine (Phase E: large-v3 model)
faster-whisper>=1.0.0

# Video Processing (Phase D: FFmpeg + NVENC — moviepy 完全廃止)
# moviepy は不要。FFmpeg がシステムにインストールされていることが必須。

# WebSocket
websockets>=14.0

# HTTP Client
httpx>=0.28.0

# File Locking (Sprint 4.2.4 C-05: evolution_log.json排他制御)
filelock>=3.13
